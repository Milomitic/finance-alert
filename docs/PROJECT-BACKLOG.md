# Finance Alert — backlog unico

Ultimo aggiornamento: 2026-09-16  
Repository: `finance-alert-cloud`  
Branch di lavoro: `cloud` (l'unico che CI costruisce e che il CD rilascia)

Questo file è il backlog operativo canonico. Ogni nuova attività deve avere un ID qui; un’attività si chiude solo quando il codice è stato pushato, sincronizzato dal CD e verificato nell’ambiente target.

## Stati

- **DONE / PROD**: implementata, pushata e verificata in produzione.
- **READY / LOCAL**: implementata e committata localmente, ma non ancora presente in produzione.
- **IN PROGRESS**: implementazione o verifica in corso.
- **OPEN**: analizzata, da implementare.
- **WAITING DATA**: strumentazione presente; servono dati reali prima di decidere.
- **BLOCKED**: il prossimo passo richiede un’azione esterna o un’autorizzazione non disponibile.

## Stato corrente

| ID | Priorità | Area | Attività | Stato | Evidenza / criterio di chiusura |
|---|---:|---|---|---|---|
| FA-086 | P2 | UI / Identita' | Logo Finance Alert e nuovo design della pagina di autenticazione | **IN PROGRESS** | Monogramma FA originale generato con ImageGen, PNG trasparente conservato e WebP 256px da 9.352 byte caricato dalla pagina. Accesso scuro a due colonne su desktop e compatto su mobile; mostra/nascondi password, errori inline accessibili, focus al primo campo mancante, stato di invio e messaggio specifico per troppi tentativi. CSS limitato alla pagina. Build e lint verdi; 10 test mirati su login/logout verdi. Collaudo browser a 1280x720, 390x844 e 320x568: logo caricato, nessuno sforamento orizzontale, validazione e focus verificati. Da chiudere con push, CI e verifica del rilascio. |
| FA-085 | P2 | Misure | I setup «ritirati» vanno contati come mancate conversioni? | **WAITING DATA** | **Decisione dell'utente (2026-09-16): per ora restano FUORI dal tasso di conversione**, come le conversioni di verso opposto (`mislinked`, escluse in ogni caso: sono un errore di registrazione, non un comportamento del setup). Il dubbio: il ritiro lo decide il sistema quando il punteggio scende, e un punteggio che scende puo' essere un fallimento arrivato prima della scadenza; se e' cosi', escluderli gonfia il tasso senza che niente lo mostri. Misurato il 2026-09-16 in sola lettura: 115 ritirati, 111 con la finestra di 28 giorni ancora aperta, 4 chiusi e **nessuno dei 4 poi scattato** — nella direzione del rischio, ma quattro casi non decidono niente (i ritirati si registrano solo dal 2026-09-14, prima venivano cancellati). **Regola decisa dall'utente:** se buona parte dei ritirati non scatta mai entro 28 giorni dall'apertura, vanno contati come mancate conversioni; se molti scattano dopo il ritiro, l'esclusione e' giusta. **Criterio di chiusura:** misura ripetuta dal 2026-10-12 (promemoria programmato `verifica-setup-ritirati`), con almeno ~30 finestre chiuse e un intervallo di Wilson sulla quota «mai scattato». Tasso al 2026-09-16: 48,6% ritirati esclusi; con i ritirati contati come mancate conversioni sarebbe sensibilmente piu' basso (~39% sui chiusi di quel giorno). |
| FA-084 | P1 | Misure | Ciclo setup → segnale → esito: conversioni mancate, di verso opposto, ed esiti misurati sul momento sbagliato | **DONE / PROD** | **Da un'analisi esterna verificata in produzione prima di agire (2026-09-16); piano in `docs/superpowers/plans/2026-09-16-ciclo-setup-segnale-esito.md`.** Rilascio verificato sul pod con immagine `472b377f` (`GIT_SHA` letto dal container, migrazione `d9e4b6a1c7f2`, stringhe nel bundle). (1) `b7c61c62`: la conversione stava solo sul ramo che CREA un alert — 95 setup attivi avevano gia' visto scattare la condizione, tutti passati dal ramo che aggiorna; e non guardava il verso — **68 conversioni su 334 erano eventi opposti**, difetto non presente nell'analisi. `convert_setups_for_event` gira su ogni rilevazione, pretende verso compatibile e barra successiva a quella d'apertura (`first_seen_bar`), e fissa sul setup data, prezzo e tono dell'evento; `mature_setup_outcomes` ne matura l'esito dalla sua barra (71 conversioni puntavano a un alert scivolato fino a 28 giorni dopo). Migrazione `c4f1a7d20e93`, simulata in sola lettura prima di scriverla e identica sul pod: 266 `legacy` (data certa solo dove l'alert non fu mai rivisto: 14), 68 `mislinked` fuori dal tasso, 85 `reconciled` senza data, anticipo ne' esito. **Tolte le conversioni opposte l'efficacia dei convertiti passa da 54,2% (45/38) a 47,4% (27/30)**: sempre una finestra indipendente, non concludente, ma il segno si inverte. (2) `fc743a7a`: «Esiti» mostrava il denominatore del tasso (695) sotto il nome dei chiusi (797); ora i chiusi con esclusi e chiusure senza ragione a schermo; «Convertiti: esito» (verde su 45 contro 38) ed «Efficacia» sono una tessera sola, colore solo se l'intervallo esclude il 50, anche sul rendimento. (3) `3c9b6196`: la maturazione caricava l'intera `ohlcv_daily` a ogni fine scansione (10,5 s, zero maturabili); il prefiltro SQL costa 0,67 s ed e' equivalente per costruzione, verificato in test e sul vivo (orizzonte 1: 3.291 alert identici). (4) `64350441`: versioni di emissione, conversione e metodo d'esito in `app/core/provenance.py`, scritte su snapshot, setup ed esiti; lo storico resta «non registrata» (4.834 esiti, 357 conversioni al rilascio). (5) `472b377f`: la riga di un convertito dice evento ed esito, e l'origine nel dialogo del segnale la barra di conversione quando differisce. Sonda di mutazione su `setup_service` e `signal_outcome_service`: sopravvissuti nuovi decisi uno per uno, due lacune chiuse con test, una duplicazione del riferimento dell'universo tolta (`44e1aec6`). ⚠️ Il messaggio di `7ee057ee` dice «3 in meno»: quel commit aggiorna solo i conteggi dei mutanti, i tre sopravvissuti non erano mai entrati in linea di base. Il collaudo in browser del percorso completo e' in FA-066, chiusa lo stesso giorno. |
| FA-083 | P1 | Misure | I numeri di Diagnostica e In formazione descrivevano una fetta, non la popolazione | **DONE / PROD** | **Segnalato dall'utente il 2026-09-16.** Due commit, verificati sul pod con immagine `e5589c0` leggendo le API vive. (1) `41a9726`: la Calibrazione diceva «esiti maturati: 3» su 4.830 righe del magazzino, perche' ricalcolava i rendimenti dei soli alert NON ARCHIVIATI — il filtro tolto agli altri consumatori il 2026-09-05 e rimasto qui. Ora legge `signal_outcomes` intero, hit market-neutral, fasce di Forza, orizzonti 5/21/63: in produzione 1.737 / 2.324 / 769 esiti. (2) `e5589c0`, **decisione dell'utente**: le misure dei setup contano TUTTI i setup del database (2.266, scope `all`) invece della sola shortlist (98), che dava 11 convertiti e «nessun esito» mentre fra 334 convertiti gli esiti erano 83 (45 positivi, 38 negativi). Titolo «Esiti — 50 setup chiusi» ora sul totale filtrato (797), e `counts_by_condition` dal server per i gruppi. Letto a schermo sulla popolazione: la Forza 90-100 non rende meglio delle fasce basse (39% a 5 giorni, 48% a 21). Test con controlli negativi su archiviati, hit assoluto e conteggi di pagina. |
| FA-082 | P2 | Verifica | La notturna di mutazione era rossa con 12 sopravvissuti nuovi | **DONE / PROD** | Commit `e2244e4` e `23180d9`. Nove erano falsi: i test di FA-061 e FA-071 non stavano in `BERSAGLI`. Quattro lacune vere, tutte casi di prova che non distinguevano (3/1/2 toni, dove i non-rialzisti erano anch'essi 3; un decaduto contro uno scaduto; nessuna media con piu' decimali; `forget` senza righe). Rimisurato con la sonda: `technical_score_service` exit 0. ⚠️ Il messaggio di `23180d9` cita `7794942`, che il rebase ha riscritto in `e2244e4`: la trappola dello SHA nello stesso push, ripetuta. |
| FA-081 | P1 | Dati | Colonne VARCHAR e date di fine: chiudere la famiglia di FA-077 | **DONE / PROD** | Commit `b235bf9`: la suite crea su SQLite un trigger per ogni `String(n)` (128), cosi' un valore troppo lungo fallisce in test come in Postgres; acceso, 2.639 test verdi; in produzione la colonna piu' vicina al tetto e' `scan_runs.phase` (28/32). Commit `8445cb3`: migrazione `a7c3e19d2b60`, data di fine alle 18 scansioni di maggio che non l'avevano (radice di FA-078); in produzione 0 righe residue, la #19 ha `completed_at` = ultimo avanzamento. |
| FA-080 | P2 | UI | La card Analyst calcolava l'upside su un prezzo diverso dalla Stock Score | **DONE / PROD** | Commit `a2eb50a`. Stesso target, due upside: Stock Score sull'ultima chiusura (55,41), Analyst su `pt.current` di yfinance (62,09, fino a 7 giorni). `price_base.last_close` e' ora il proprietario unico; `price_target.current` e' l'ultima chiusura con `current_as_of`. Verificato sul pod: AAPL 331,34 del 15/09 in entrambe le card. |
| FA-075 | P2 | UI | Compattare Stock Score e uniformare le etichette di freschezza | **DONE / PROD** | Commit `aa3b897` pushato; CI verde (backend, frontend, gate UI browser reale, Trivy) e GitOps `9382ada`. Pod `finance-alert-finance-alert-0` verificato Ready/Running con immagine `ghcr.io/milomitic/finance-alert:aa3b897e74f4464c0f87db09b64e8cd2b7b3d23a`; Argo `Synced/Healthy`, operazione `Succeeded` il 2026-09-15. Giudizio sopra il gauge e confronto settore sotto, nella colonna sinistra; rischio e storico a destra. Confidenza e tempo dal calcolo sulla riga del titolo. Oltre 1920px, piu' spazio al pannello destro e a Stock Score. Tolto «aggiornato» dalle eta' dei dati condivise, dalla dashboard e dai tooltip del prezzo live. Collaudo browser con dati di esempio equivalenti alla schermata: da 406,5 a 382,8px di altezza a 350px di larghezza; nessun overflow o titolo troncato a 300/350/390px, anche subito dopo il calcolo. |
| FA-076 | P1 | Infrastruttura | Il disco del nodo e' oltre la soglia critica, e l'allarme suona da giorni | **DONE / PROD** | **Scelta dell'utente: opzione 2, applicata il 2026-09-15.** Commit `c36a1b4`. Il boot volume era da 50 GB, non 30: Oracle Linux divide il PV LVM in `root` (29,5 GB) e `/var/oled` (15 GB, ~300 MB usati, non restringibile perche' XFS). `terraform apply -target=oci_core_instance.k3s` nel container del repository, dopo un piano che diceva update in-place con 0 da distruggere e con lo stato salvato prima: 50 -> 100 GB in 26 s, **nessun riavvio** (uptime del nodo invariato dal 17 luglio). Poi `oci-growfs -y` sul nodo: partizione sda3, PV, LV root e XFS, `/` da 30 a 83 GB. **Verificato**: `df` 86% -> 31% con 58 GB liberi; tutti i pod pronti e `/api/health` ok; Prometheus 69,3% libero su `/` e `FinanceAlertNodeRootFilling` inattivo alle 16:58:54 UTC; il `terraform plan` successivo dice «No changes». Il default di `boot_volume_gbs` e' 100, perche' un piano a 50 fallirebbe (OCI non restringe un volume). Margine Always-Free: 100 GB su 200. Procedura e trappole in CLAUDE.md. Misurato il 2026-09-15: **86% usato** (26,9 GB su 31,6 GB), picco a 7 giorni 86,0%. `FinanceAlertNodeRootFilling` (critico, sotto il 15% libero) ha passato **399 campioni in stato firing** negli ultimi 7 giorni e Alertmanager ha consegnato 63 notifiche: oscilla attorno alla soglia. CLAUDE.md lo dava all'82% e poi al 79% il 9 settembre. Dove va il disco, sul solo filesystem radice: containerd 8,66 GB (snapshot 6,53, contenuti 2,11), PVC 4,32 GB (Prometheus 2,53 — gia' al suo tetto di 2,5 GB, quindi non cresce —, Postgres 1,19, app 0,43, Loki 0,12, Grafana 0,05), `/usr` 4,75 GB, **`/.swapfile` 4 GB con 323 MB usati**, `/var/cache` 0,95, `/opt` 0,53, log 0,40. Nessun consumatore fuori controllo: la crescita dal 9 settembre e' containerd, che si ripulisce da solo ma non sotto la soglia. **Tre strade, e nessuna e' stata presa perche' tutte toccano il nodo che fa girare tutto**: (1) ridurre lo swapfile da 4 a 1 GB libera 3 GB subito e si annulla, al costo di margine sotto pressione di memoria; (2) allargare il volume di boot e' la correzione strutturale (Terraform + `growpart`/`xfs_growfs` online); (3) abbassare `retentionSize` di Prometheus libera al massimo 1 GB e accorcia la storia delle metriche. ⚠️ **NON potare le immagini a mano**: CLAUDE.md registra che containerd le raccoglie da solo, e la potatura non cambia il problema strutturale di un disco da 30 GB per tutto lo stack. |
| FA-077 | P0 | Motore | Ogni scansione in produzione falliva dal 2026-09-14 19:53 UTC | **DONE / PROD** | Tre difetti, tre commit. (1) `33ad6a7`: FA-061 (`8e6b035`) scriveva il tono `undetermined` (12 caratteri) in `stock_setups.tone` `String(8)`; Postgres lo rifiuta (`StringDataRightTruncation`), SQLite no. Undici scansioni fallite di fila, compresa la notturna; nessuna riga con quel tono in produzione. Migrazione `5e2f0b7c9d41` (colonna a 16; il downgrade rifiuta se trova toni che non entrano). Test SQLite sulle costanti contro la lunghezza della colonna, controllo negativo eseguito; test Postgres col rifiuto sullo schema di prima e l'accettazione dopo. (2) `8a0b25f`: il ramo `except` di `run_tracked_scan` leggeva `run.id` prima del rollback, crollava con `PendingRollbackError` e la riga risultava «heartbeat fermo» invece dell'errore vero; test rossi prima, verdi dopo. (3) `9301f5d`: su Postgres `ORDER BY completed_at DESC` metteva in testa una delle 18 esecuzioni `success` di maggio senza data, quindi ogni ricreazione del pod lanciava una scansione completa (83 ricreazioni in 7 giorni, da 5 a 22 scansioni al giorno) e la finestra di recenza non si allargava mai; `last_successful_completed_at` con `MAX`, censimento del sorgente con controllo negativo, test Postgres che esegue la forma vecchia. **Verificato in produzione**: pod su `9301f5d`, Alembic `5e2f0b7c9d41`, colonna a 16; scansione #633 `success` (937 titoli, 14:17-14:33 UTC), 242 setup `undetermined` attivi, `last_successful_completed_at` = 14:33:09 UTC. Lezioni in CLAUDE.md (`a8ce5dd`). |
| FA-078 | P2 | Infrastruttura | A ogni avvio del pod ripartivano Dataroma e SEC 13F | **DONE / PROD** | Commit `fd4424c`. `filings_refresh_is_stale` guardava solo `MAX(created_at)` dei filing, cioe' l'ultimo filing NUOVO, e i 13F sono trimestrali: da Loki, 87 avvii in 7 giorni, 87 recuperi, zero «fresh» (ultimo filing nuovo il 22 agosto). Ogni job scrive ora un marcatore del refresh riuscito in `fetch_cache` (`refresh:dataroma`, `refresh:sec_13f`), anche a zero filing nuovi; fresco se c'e' un filing nuovo o se entrambe le fonti hanno un refresh riuscito negli ultimi 8 giorni. Due controlli negativi eseguiti. **Verificato in produzione** sul rilascio `2ff388c`: al primo avvio `last scan is fresh — skipping catch-up` (conferma anche `9301f5d`) e un ultimo recupero istituzionale, finito bene, che ha scritto i due marcatori (15:51 e 15:52 UTC); `filings_refresh_is_stale` letto nel pod subito dopo = `False`, quindi l'avvio successivo non lo rilancia. |
| FA-079 | P2 | Infrastruttura | Job batch: inventario e margini di ottimizzazione | **DONE / PROD** | **Applicate tutte e sette le proposte, su richiesta, e verificate in produzione sul rilascio `38daf91`** (CI verde, `GIT_SHA` nel pod). #1 `9236a0d` scansione delle 23:30 solo lun-ven, `FinanceAlertNotScanning` silenziata da sabato 00:00 a lunedi' 20:00 UTC (confini valutati su Prometheus vero; regola applicata). #2 `c255323` la scansione delle 18:30 non ricalcola gli score ne' cattura lo storico (la cattura giornaliera vince la prima). #3 #4 #5 `26b1086` FRED 5 passate feriali dopo i rilasci, sonde veloci ogni 15', `db_backup` solo su SQLite. #7 `8a6a772` metrica `finance_alert_run_failure_streak` e allarme `FinanceAlertScansFailing` (>= 2). #6 `38daf91` una scansione uccisa da un riavvio si rilancia da sola (attesa di 150 s sull'heartbeat, o subito se chiusa all'avvio) invece di far aspettare lo spegnimento, che con una replica avrebbe tenuto giu' l'app. ⚠️ **E un difetto trovato strada facendo, `724c05e`**: i 17 `CronTrigger` prendevano il fuso della MACCHINA, non quello di `BackgroundScheduler` — dal passaggio al cloud ogni job girava due ore dopo l'orario scritto (scansioni alle 18:30 e 23:30 UTC). Emerso perche' la CI di `26b1086` era rossa sulle finestre di FRED e verde su Windows; ora `_cron()` passa Europe/Rome e il test simula una macchina UTC. **Verificato nel pod**: prossime esecuzioni dello scheduler vivo `scan_alerts` mar 23:30 Roma, `scan_alerts_eu_close` mer 18:30, `refresh_fred` mar 23:15 (cron lun-ven 7,15,16,17,23), `send_digest` 08:00, `refresh_catalog` sab 03:00, `health_probes_fast` ogni 15'; `db_backup` senza trigger ne' prossima esecuzione (compare solo con le statistiche storiche persistite); all'avvio `last scan is fresh` e `institutional filings are fresh` (conferma anche FA-078 al secondo avvio); `finance_alert_run_failure_streak{kind="alerts_scan"}` = 0 in Prometheus; `FinanceAlertScansFailing` e `FinanceAlertNotScanning` caricate, `inactive`, health ok. Il recupero #6 non e' stato esercitato dal vivo (nessuna scansione interrotta): lo coprono i test, con due controlli negativi. Nota: il rilascio e' arrivato alle 18:37 di Roma, dopo le 18:30, quindi oggi la scansione serale salta e la prossima e' quella delle 23:30. Inventario originale, misurato il 2026-09-15. **Tre luoghi**: 19 job nello scheduler dell'app (Europe/Rome), 2 CronJob nel cluster (parita' immagine ogni 15', drill di ripristino lun 04:00, ~2'), backup Postgres CNPG 02:00, 1 workflow notturno GitHub (trivy sull'immagine dispiegata + mutazione). **Costi reali** (ultima durata / esecuzioni): scansione dopo la chiusura UE 718 s, refresh catalogo sab 437 s, istituzionali sab 130 s + SEC 13F 58 s, FRED 37 s ogni 2 h (810 esecuzioni), riparazione buchi OHLCV 17 s ogni 6 h, sweep live 10 s ogni 75 s (77.563), utili imminenti 6 s/h, sonde salute 3-4 s ogni 5'/30'. Una scansione riuscita ~12': ricalcolo score 339 s, valutazione 209 s, download prezzi 82 s, statistiche di settore 61 s. CPU del pod: picchi 0,4-0,5 core durante le scansioni, quasi zero la notte, limite 1,5. **Gia' corretti**: scansione a ogni avvio del pod (`9301f5d`, era 5-22 al giorno) e scraping istituzionale a ogni avvio (`fd4424c`, 87 in 7 giorni). **Proposte, nessuna applicata**: (1) scansione delle 23:30 anche sabato e domenica senza barre nuove — passarla a lun-ven risparmia ~24'/settimana, ma `FinanceAlertNotScanning` (26 h) va portata a ~74 h nel fine settimana; (2) il ricalcolo score (~6') gira in ENTRAMBE le scansioni feriali mentre i fondamentali hanno TTL 7 giorni — farlo solo in quella delle 23:30; (3) FRED ogni 2 h contro serie che cambiano al massimo una volta al giorno — 4 passate al giorno o finestre sui rilasci; (4) sonde salute veloci ogni 5' (288 al giorno, alcune su fonti con quota) — 15'; (5) `db_backup` gira ogni notte ma su Postgres non fa niente — non registrarlo fuori da SQLite; (6) **i rilasci interrompono le scansioni** (83 ricreazioni del pod in 7 giorni; la #632 uccisa oggi a meta' ricalcolo) — uno spegnimento che aspetti la fine della scansione, o una finestra di sync ArgoCD; (7) **nessun allarme su una SERIE di scansioni fallite**: undici fallite di fila il 14-15/09 e `FinanceAlertNotScanning` sarebbe scattato solo a 26 h — un allarme su N fallimenti consecutivi; (8) il notturno GitHub fissato alle 03:17 UTC parte fra le 08:20 e le 08:55 (coda dei runner condivisi): accettabile, da sapere. |
| FA-001 | P0 | Sicurezza | Proteggere `/metrics` dall’ingress pubblico con allowlist loopback | **DONE / PROD** | Commit `cb524d5` pushato; probe esterno 403, health 200, Prometheus interno `up=1`. |
| FA-002 | P1 | Backup | Portare la retention PostgreSQL da 7 a 30 giorni | **DONE / PROD** | ObjectStore pg-backups: retentionPolicy=30d verificata il 2026-09-09. La storia preesistente inizia il 2 settembre: 30d e la policy, non 30 giorni di backup retroattivi. |
| FA-003 | P1 | Backup | Migrare dal Barman integrato CNPG al Barman Cloud Plugin prima di CNPG 1.31 | **DONE / PROD** | Plugin chart 0.8.0/v0.15.0 installato, `pg-1` 2/2 Ready, WAL archiviati, `ScheduledBackup pg-daily` con `method=plugin` e backup `pg-plugin-verify-20260909` **completed** in 3m34s. Restore separato riuscito: cluster sano, dati verificati (stocks/alerts identici al live, ohlcv 2.476.821 contro 2.476.822 perche il live ha acquisito una barra dopo il backup; 28 tabelle, alembic `a3f71c9d2e40`, `fa_app` non superuser). Le risorse temporanee di restore e backup sono state eliminate e verificate come `NotFound`. |
| FA-004 | P1 | Osservabilità | Aggiornare gli alert ai metric name del plugin | **DONE / PROD** | PrometheusRule applicata, plugin scrape `up=1`, e la verifica che conta e fatta (2026-09-09): le tre serie `barman_cloud_cloudnative_pg_io_*` **esistono** in Prometheus, entrambe le regole sui backup le interrogano, tutte le 14 regole del gruppo valutano `ok/inactive` e l'ultimo backup disponibile risulta di 12,7 ore fa. Una regola che punta a un nome inesistente resta verde per assenza di serie, non per salute: e il guasto gia documentato in CLAUDE.md. |
| FA-005 | P2 | Kubernetes | Rimuovere `spec.monitoring.enablePodMonitor` deprecato | **DONE / PROD** | PodMonitor finance-alert-postgres presente in monitoring, scrape up=1; enablePodMonitor=false restituito dal default del server. |
| FA-006 | P2 | Performance | Analizzare i percorsi lenti sui percentili reali | **IN PROGRESS** | **Misurata il 2026-09-16** su 3 giorni dopo il cambio delle fasce (le finestre a 7 giorni erano falsate dalla serie senza fascia 5 s e dai riavvii). Nessuna richiesta oltre 30 s; il percorso lento e' uno solo, `/api/stocks/quotes`: mediana 5,5 s, p95 9,5 s, 57% sopra 5 s, 102 batch su 121 in 24 ore oltre la scadenza di 6 s, fino a 50/50 serviti dalla cache. Prima correzione `e01ebcf` (sweep in un pool suo): **insufficiente**, dopo il rilascio restavano batch 50/50. Misura nel pod: 5 thread = ~8 quotazioni/s, una pagina ne chiede ~240 insieme. Seconda correzione `8cd929a`: niente riaccodamento dei titoli gia' in coda, e chi ha un valore noto lo riceve subito. **Chiusura:** con un giorno di borsa sull'immagine `8cd929a`, mediana di `/api/stocks/quotes` sotto 1 s e righe «batch deadline» limitate ai titoli senza alcun valore. |
| FA-007 | P2 | Frontend | Introdurre RUM per Web Vitals LCP/INP/CLS | **WAITING DATA** | `web-vitals` ufficiale per CLS/INP/LCP e route a cardinalita finita sono in produzione sull’immagine `42693fb`. Servono sessioni browser reali per popolare i percentili e validare il payload. |
| FA-008 | P2 | Frontend | Ridurre e presidiare i finding ESLint React Hooks | **DONE** | Commit `bd5c11a` e `5d01d6d` (2026-09-13). **Ricontato il 2026-09-15: `npm run lint` esce 0 con zero finding** — erano 52 l'11 settembre. `lint:hooks` gatta sette regole, fra cui `set-state-in-effect` ed `exhaustive-deps`, entrate solo DOPO essere state misurate a zero. `only-export-components` e' a zero e NON e' gatta di proposito: rompe il Fast Refresh, che costa a chi sviluppa e non a chi usa. La pulizia ha trovato difetti veri — l'evidenziazione della ricerca su una lista gia' accorciata, lo spessore applicato a una sola linea del MACD — e nessuna delle due regole si chiude nel modo ovvio: dettagli in CLAUDE.md, Test commands. ⚠️ Questa riga ha detto «OPEN, 52» per due giorni dopo la chiusura: un conteggio copiato invece che ricontato. |
| FA-009 | P1 | UX | Correggere etichette valuta/prezzo nelle posizioni | **DONE / PROD** | Prezzi e P&L usano la valuta nativa con fallback USD; codice incluso nell’immagine `42693fb`, build e suite frontend verdi. |
| FA-010 | P1 | UX | Rendere visibili e recuperabili gli errori delle mutazioni | **DONE / PROD** | Hook posizioni con toast successo/errore e dettaglio API; form trade mantiene errore inline; codice incluso nell’immagine `42693fb`, build e suite frontend verdi. |
| FA-011 | P1 | UX mobile | Focus trap, ESC, scroll lock e restore del drawer | **DONE / PROD** | Collaudata su browser vero l'11 settembre, viewport telefono 375x812, con eventi reali di tastiera e mouse. **Passa su tutti e cinque i punti**: scorrimento bloccato (`body overflow hidden`, rilasciato alla chiusura), fuoco che entra su «Chiudi menu», trappola in avanti (Tab dall'ultimo torna al primo) e all'indietro (Shift+Tab dal primo va all'ultimo), ESC che chiude, e il fuoco restituito a «Apri menu» invece che al `<body>`. ⚠️ **Un solo scostamento, ed era la forma peggiore**: il drawer si COMPORTA da modale e non lo dichiarava — niente `role` ne `aria-modal`, quindi un lettore di schermo non annuncia un confine e l'utente sente la trappola come «il fuoco non si muove piu» invece che come «sono dentro un pannello». Funziona per chi guarda e confonde chi ascolta. **axe non lo prende**: un `div` senza ruolo non viola nessuna regola, perche e un'ASSENZA e non una dichiarazione sbagliata. Corretto e pinnato in `Layout.a11y.test.tsx`.
| FA-012 | P1 | UX/search | Stati errore e semantica combobox | **DONE / PROD** | Stato errore con retry e ruoli `combobox`/`listbox`/`option` implementati e inclusi nell’immagine `42693fb`; build e suite frontend verdi. |
| FA-013 | P2 | UX/navigation | Preservare filtri/calendar/search nell'URL e migliorare il 404 | **DONE / PROD** | Collaudata su browser vero l'11 settembre. Cronologia e 404 passano: l'ordinamento finisce in `?sort_by=market_cap&sort_dir=desc`, e tornando indietro da /calendar lo stato non e solo nell'indirizzo ma **riapplicato** (NVDA, GOOGL, AAPL in cima); il forward torna al calendario con la sua data; il 404 rende «Errore 404 · Pagina non trovata» con due uscite. ⚠️ **Il collaudo ha trovato il difetto che la voce chiedeva di cercare**: `CalendarPage` scriveva la data in UTC (`toISOString().slice(0,10)`) e la rileggeva in locale (`new Date(\`${d}T12:00:00\`)`), dentro lo stesso componente. Non e una questione di quale fuso sia giusto: scrivere con un riferimento e leggere con un altro sposta il giorno per una finestra larga quanto lo scostamento — a Roma 00:00-02:00, a New York **la sera**, cioe quando un calendario si guarda davvero. E a mezzogiorno i due coincidono ovunque, che e' perche il difetto sopravvive a una prova manuale in orario di lavoro. Proprietario: `lib/localDate.toLocalIsoDate`; il test forza `TZ` per poter fallire. ⚠️ Le altre tre occorrenze di `toISOString().slice(0,10)` **non** sono state toccate: due confrontano `as_of_date`, che nasce dalla barra OHLCV e vive nel fuso del MERCATO — li il riferimento giusto e una domanda aperta — e la terza e un timbro in un nome di file. Il 404 non intitolava la scheda, perche il titolo per rotta lo imposta `Layout` e `path="*"` e montato fuori: corretto li, dove nessuno lo sovrascrive.
| FA-014 | P2 | Sessioni | Revoca server-side e logout tra schede | **DONE / PROD** | Tabella `revoked_sessions` (migrazione `50c3faa68285`) piu idratazione in `lifespan`. La lista viveva in un dizionario di processo: un riavvio riportava in vita ogni token di cui qualcuno aveva fatto logout, per i **sette giorni** di `session_max_age_days`, e ogni deploy e un riavvio. Solo il digest SHA-256 finisce a tabella, mai il token. **Le letture restano in memoria di proposito**: `read_session_token` gira a ogni richiesta autenticata e `/api/health` da solo e 18k delle ~20k giornaliere, quindi una SELECT per richiesta pagherebbe una domanda la cui risposta e quasi sempre no. ⚠️ Questo NON risolve piu repliche: la revoca raggiunge subito il database ma la memoria dell'altra replica lo impara al proprio riavvio. Oggi la replica e una; per due servirebbe una rilettura a TTL o un pub/sub. 22 test. |
| FA-015 | P3 | Logging/storage | Guardrail dimensione Loki e monitoraggio disco nodo | **DONE** | Commit `05de476`, applicato al cluster e verificato: Prometheus ha ricaricato le regole, `FinanceAlertLokiPVCFilling` non c'e' piu' e `FinanceAlertNodeRootFilling` resta caricata. ⚠️ **Non era bloccata da fuori, e la diagnosi era sbagliata due volte.** Il kubelet le `volume_stats` le espone — dodici serie misurate il 2026-09-15 — e la regola era caricata, sana e `inactive` per sempre perche' il selettore `persistentvolumeclaim=~"loki.*"` e' una regex ANCORATA e il PVC si chiama `storage-loki-0`. Correggere la regex sarebbe stato peggio: su `local-path` un PVC e' una directory sul disco del nodo, e ogni PVC del cluster riporta 26,83 GB usati su 31,6 GB — identici a `df /` — mentre Loki ne occupa 122 MB. La regola «corretta» avrebbe letto 0,85, oltre la soglia, e detto «PVC Loki oltre l'80%» con il rimedio sbagliato. Rimossa: la capienza vera e' quella del nodo, gia' sorvegliata, e Loki e' limitato nel tempo (720h). Un test impedisce che una regola torni a leggere `kubelet_volume_stats`, con il controllo negativo eseguito. ⚠️ La guardia sul nodo che doveva restare **sta suonando**: vedi FA-076. |
| FA-016 | P2 | Prodotto | Decomporre le 72 proposte dell’audit UI/UX | **DONE** | Riconciliazione completata il 2026-09-10: tutte e 72 le voci hanno ora **Stato** ed **Evidenza** in [frontend-ux-audit-2026-09-09.md](frontend-ux-audit-2026-09-09.md). Esito: 34 gia presenti, 31 mancanti, 5 parziali, 1 in attesa di dati, 1 rifiutata (UX-061 watchlist, rimossa deliberatamente). **Quasi meta della roadmap era gia costruita**, quindi pianificarla come lavoro nuovo avrebbe riscritto codice in produzione. Due verdetti sono stati corretti in corsa da un grep senza confini di parola: `mute` sta dentro `text-muted-foreground`. Le 31 mancanti sono candidate, non lavoro impegnato: entrano qui con un ID quando l'utente le chiede. |
| FA-017 | P2 | Sicurezza | Triage dei finding Bandit e hardening dei casi confermati | **DONE / PROD** | Validazione URL HTTP(S) aggiunta; classificazione in [docs/security-bandit-triage.md](security-bandit-triage.md). Nessun high; audit npm/pip e CI 34366785799 verdi; codice deployato nell’immagine `42693fb`. |
| FA-020 | P1 | Accessibilita | Lingua del documento, link di salto, nome del logout e titolo per rotta | **DONE / PROD** | `lang="it"`, link "Salta al contenuto" primo elemento focalizzabile, label logout `sr-only` e `document.title` derivato da `NAV`; 7 test dedicati, suite frontend 290 pass; immagine `42693fb` verificata in produzione. |
| FA-022 | P2 | UX/screener | Viste salvate: filtri, ordinamento e colonne insieme | **DONE / PROD** | Estende i preset esistenti invece di affiancarli. Formato versionato con migrazione in LETTURA: i preset gia nel browser sono oggetti di soli filtri senza marcatore e continuano a funzionare, applicandosi con l'ordinamento PREDEFINITO della tabella e non con quello corrente. 16 test fra `screenerViews.test.ts` e `StockFiltersCard.views.test.tsx`; suite frontend 314 pass. Commit `f88a8d5`, CI verde, immagine rilasciata. |
| FA-025 | P1 | UX/valuta | La valuta di quotazione sul dettaglio titolo | **DONE / PROD** | 312 titoli su 1010 non sono in dollari e la pagina cablava `$` su ogni prezzo: per un terzo dell'universo il simbolo non era ambiguo, era **sbagliato**. `lib/money.ts` unifica i TRE formatter che esistevano (lo screener aveva una mappa e faceva bene, PositionsPage un helper `Intl`, il dettaglio niente). 26 punti convertiti: prezzo, variazione, chiusura precedente, legenda del grafico, price alert, range 52w, holding ETF, target analisti, EPS, grafico trimestrale. Migrazione `8fc285de13e2` per sei righe LSE con etichetta `GBp` stantia (i PREZZI erano corretti: tutte e 99 le `.L` hanno `ohlcv_in_pounds`), e `seed_service` normalizza al confine perche il prossimo import non le rimetta. 11 test backend, 42 frontend. |
| FA-023 | P2 | UX/grafici | Marker dei segnali leggibili e pan che si ferma invece di resettare | **DONE / PROD** | Commit `9363ed9`. I marker passano alla palette direzionale rosa/emerald imposta da CLAUDE.md (rosso/verde significa "rotto"), guadagnano un gradino di dimensione e **portano il conteggio** quando piu segnali cadono sulla stessa barra: prima tre segnali e uno disegnavano lo stesso glifo, quindi i giorni piu affollati sembravano i piu tranquilli. La dimensione NON dipende dalla Forza, ed e fissato da un test: sarebbe la stessa affermazione della rampa di rischio rimossa dal playbook, dove la banda 90-99 ha realizzato 42,3% contro 52-53%. Il clamp e riscritto per **far scorrere la finestra senza mai cambiarne la larghezza** — la vecchia versione tappava i due bordi in modo indipendente, allargava la finestra all'intera serie e lo zoom si azzerava. 11 test in `chartClamp.test.ts`, che prima non ne aveva pur governando tre pannelli. |
| FA-024 | P2 | Prodotto | Statistiche su rendimenti ed efficacia nelle pagine setup ed esiti | **DONE / PROD** | Commit `3a6ee82`, immagine `3a6ee82d` verificata in produzione con le cinque Application `Synced/Healthy`. `conversion_stats` guadagna rendimento **market-neutral e assoluto in coppia** (pubblicarne uno solo lascia che un setup rialzista si intesti la deriva del mercato), mediana in testa e media accanto, tasso di efficacia con intervallo di Wilson dimensionato su `independent_blocks` e non sulle righe, piu `by_detector`. Il pannello per famiglia rende **il tasso con la sua banda contro il 50%**, non una barra del tasso: una banda che attraversa il 50 diventa grigia e legge "non concludente". 13 test backend, 11 frontend. |
| FA-026 | P1 | Dati | Il market cap e nella valuta di quotazione e lo screener ci ordina | **DONE / PROD** | Decisione D-1 del piano, presa l'11 settembre: **opzione C — ordinare in USD, mostrare in valuta nativa**. Commit `a4d8e20`, sette job CI verdi. ⚠️ **Non era una rietichettatura**: misurato su 984 titoli con capitalizzazione, la top 10 in valuta nativa e quella convertita **non hanno un titolo in comune** — la prima e fatta di dieci nomi coreani, perche una cifra in KRW e circa 1300 volte piu grande. Perche la C: la colonna esiste per ORDINARE quindi l'ordinamento non puo rompersi (esclude la B), e convertire a schermo la renderebbe l'unico numero convertito di una riga dove il prezzo resta nativo (esclude la A). L'espressione vive nel SORT come `DIVARIO_EXPR`, ed e una FUNZIONE e non una colonna perche i tassi si muovono. ⚠️ I tassi si leggono SOLO da cache: `_get_rate` farebbe una chiamata yfinance per valuta su cache fredda, cioe fino a nove round-trip dentro una query di lista. Valuta ignota -> NULL e in fondo in entrambi i versi, mai parita. `fmtMc` esisteva in tre copie che cablavano `$`; ora `lib/money.formatCompactMoney`. ⚠️ Il controllo sulla sorgente ha trovato due casi che sono stati **verificati invece che corretti**: `fmtDollar` e giusto (`dollar_volume` e convertito con `to_usd` nel backend, letto non assunto) e `bigUsd` formatta valori di bilancio in valuta di rendicontazione, non dichiarata nel payload. **Verificato a schermo l'11 settembre**: ordinando desc escono NVDA, GOOGL, AAPL, GOOG, MSFT, AMZN, AVGO, TSM; HSBC legge `HK$2.49T` con `≈ $319.0B` al passaggio (l'audit la trovava a `$2.86T`); e ASML `€533.7B → $620.1B` sta sopra 0700.HK `HK$3.86T → $494.5B`, cioe l'ordinamento usa davvero i dollari. |
| FA-027 | P1 | UX/dettaglio | Il dettaglio titolo perde titoli e valori a 1440px | **DONE / PROD** | Commit `66aa10c`, incluso nel run verde di `328e264` (sette job). Due difetti con la stessa forma: quando lo spazio manca, cede l'informazione che dice COSA stai guardando. (a) `[2fr_1fr]` lasciava 375px alla colonna destra, spezzata in due schede: ~133px di contenuto utile, da cui i valori disegnati SOPRA le etichette. ⚠️ **Il commento sopra quella griglia descriveva gia il difetto** — «side by side these two got ~170px each... the Qualità gauge collided with its risk badge» — e lo risolveva solo sotto `sm`, mentre la colonna e stretta da `lg` in su. Una correzione parziale che avvalorava l'idea che il caso fosse chiuso. `lg:grid-cols-1` porta da 133px a 375px, piu di quanto abbia il mobile, dove la stessa scheda e sempre stata leggibile. (b) `SectionTitle` aveva lo slot destro `shrink-0` e l'etichetta `truncate`: cedeva sempre e solo il titolo, da cui `F` per `FUNDAMENTALS` mentre il timestamp accanto restava intero. `flex-wrap` manda sotto la chrome e tiene il nome. Il componente e canonico, 47 schede lo usano, quindi la modifica e scelta per non cambiare nulla quando lo spazio basta. ⚠️ jsdom non calcola gli stili: i quattro test fissano la struttura, non il risultato a schermo, e lo dicono. Confermati rossi. Resta la verifica a schermo su `/stocks/DG` a 1440. |
| FA-028 | P2 | UX/dashboard | La barra indici e vuota su desktop e viva su tablet e mobile | **CHIUSA — NON RIPRODOTTA** | Spike S-1: il piano chiedeva di riprodurre PRIMA di ipotizzare la causa, e la riproduzione dice che **la premessa e falsa**. Campionando la striscia nello stesso istante a 1440, 768 e 375 il risultato e **identico** — 28 piastrelle, 2 senza valore, stesso contenuto — quindi non e condizionata dal viewport. ⚠️ Lo stato vuoto e **temporale**: a cache fredda la striscia non esiste per ~3 secondi (non rende segnaposto `n/d`, proprio non c'e), poi si popola. E l'acquisizione dell'audit fotografava il **desktop per primo** in una sessione sola, quindi coglieva sistematicamente lo stato piu freddo — «due viewport su tre funzionano» non escludeva la sorgente, misurava l'ordine degli scatti. ⚠️ **Due mie misure intermedie erano sbagliate e vanno dette**, perche sarebbero finite qui come scoperte: «tutti e 14 gli asset senza prezzo» leggeva `asset.price`, che non esiste — il prezzo sta in `asset.quote.price` — e «Hang Seng non si risana in 30 secondi» era la pane del browser in `visibilityState: hidden`, dove `refetchIntervalInBackground: false` sospende il poll **per scelta giusta**. Nessun lavoro fatto, deliberatamente: correggere qualcosa qui significherebbe inseguire un difetto che la riproduzione non trova. Resta annotata un'osservazione contigua, NON promossa a voce: una piastrella senza valore porta il motivo solo nel `title`, che su un telefono non si puo aprire — stessa classe di FA-043, ma su una striscia densa e da progettare, non da infilare. |
| FA-029 | P2 | UX/correlazioni | Una posizione non sa da quale segnale e nata | **DONE / PROD** | Chiusa da tre commit, non da uno: `a2d1c82` (posizione -> segnale, piu la geometria) e `9e530e1` (setup -> segnale). Il collegamento non era un link: la lista alert e paginata e il segnale che ha aperto una posizione di due mesi fa non sta in nessuna pagina che l'utente stia guardando, quindi e servito `GET /api/alerts/{alert_id}`. Due decisioni che il codice da solo non spiega: la forma della riga e stata **estratta**, non riscritta — due percorsi che costruiscono a mano lo stesso dict divergono in silenzio, e il dialogo mostrerebbe meno cose aperto da una posizione che dalla lista, quindi il test confronta i due payload campo per campo; e l'endpoint per id **non** filtra su `archived`, perche un segnale archiviato ha comunque generato la posizione che si sta guardando. Sul secondo anello il pulsante compare **solo sulle righe convertite**: una scaduta ha `converted_alert_id` nullo per costruzione, e un pulsante li suggerirebbe che ogni attesa si sia risolta in qualcosa. ⚠️ La riga ha dovuto cambiare forma — un controllo interattivo annidato in un `<a>` e HTML non valido e da tastiera l'invio attiva l'ancora, non il pulsante: difetto invisibile a schermo e invisibile a qualunque test che guardi solo i ruoli, quindi il test guarda l'annidamento. Sette job CI verdi sul run 34606029076. §7.1 e §7.2. |
| FA-030 | P1 | Dati | La finestra 52 settimane e l'intera storia su 5 timeframe su 6 | **DONE / PROD** | Commit `633ebc2`, sette job CI verdi, pod Ready sull'immagine corrispondente. La finestra usa le chiusure delle ultime 52 settimane indipendentemente dal timeframe; il test parametrico sui sei timeframe e stato **verificato rosso prima e verde dopo**, e prima non esisteva alcuna asserzione sul blocco — per questo il difetto e sopravvissuto al rename del vocabolario da `2faee27` in poi. ⚠️ La meta pericolosa non era il 4.40 su ^GSPC, che si autodenuncia, ma i 60 giorni etichettati «52 settimane» su 5m/30m: plausibili, quindi creduti. Resta la verifica a schermo: su `/markets/^GSPC` il 52W low deve differire dal Range low e non valere 4.40 in nessuno dei sei timeframe. |
| FA-031 | P1 | Dati | Il macro sbaglia la scala di mille volte | **DONE / PROD** | Commit `44ce2ef`, sette job CI verdi, pod Ready sull'immagine `c2eb5b95`. **Verificato sui dati veri**: migrazione `c6b2f93a1d47` applicata, PAYEMS `thousands`, GDPC1 `billions`, RSAFS `millions`, e l'ultimo PAYEMS memorizzato e 159.075 migliaia, cioe 159,1 milioni di occupati — la pagina rende ora `159.1M` invece di `159.1K`. Tre difetti, una causa comune: **l'unita era nel payload e nessuno la leggeva**, perche l'API esponeva `unit="level"` sia per una serie in migliaia sia per una in miliardi. Ora `value_kind` e `source_scale` sono due campi. ⚠️ Una scala ignota non viene indovinata: si mostra il numero memorizzato senza trasformazione, come `lib/money.ts` fa senza valuta. Il grafico distingue livello e variazione — linea contro barre, asse che non forza lo zero — e dichiara che la variazione e una trasformazione NOSTRA. E `release_date`, costruita dal periodo osservato, e diventata tre campi con la pubblicazione a `null` finche' non arriva dal calendario. Otto test, confermati rossi; fra questi che i nomi vecchi NON esistano piu, perche un frontend che leggesse `unit` otterrebbe `undefined` e il difetto tornerebbe in silenzio. Resta la verifica a schermo su `/macro/3`. |
| FA-032 | P1 | Dati | Variazione di quote stampata con l'unita del peso | **DONE / PROD** | Commit `225b11d`, sette job CI verdi. Contratti distinti `shares_change_pct` e `portfolio_weight_delta_pp`. ⚠️ Il componente `AllocationBars` era **corretto**: documentava la sua prop come peso in punti percentuali e la formattava cosi. Sbagliava il chiamante, che le passava `qoq_change_pct`, cioe quote — quindi "sistemare AllocationBars" sarebbe stata l'istruzione sbagliata. Il peso si confronta solo col filing immediatamente precedente; senza peso comparabile resta `null`, senza saltare un trimestre. L'invariante e in un test: un delta di peso oltre 100pp viene rifiutato, perche una percentuale di un intero non puo variare di piu di 100 punti e a schermo ce n'erano tre. Test verificati rossi prima e verdi dopo. |
| FA-033 | P1 | Dati | Quattro prezzi base per lo stesso titolo, nessuno dichiarato | **DONE / PROD** | Commit `9406b54` pushato. Lo stesso target rendeva +11.5% e +4.3% sulla stessa pagina; le basi erano quattro e nessuna diceva quale fosse, quindi la contraddizione non era leggibile — sembravano due misure diverse invece che la stessa misura su due basi. ⚠️ La correzione **non** e imporre una base sola: obbligherebbe ogni consumatore a dipendere dal prezzo live, che ha un breaker e puo essere STALE. Ogni confronto dichiara la propria, come i tassi dichiarano il denominatore. `target_upside_pct` viaggia con `upside_base_price` e `upside_base_as_of`, e la base compare accanto alla percentuale, non solo nel tooltip. Il test che conta e che la coppia non si separi mai: `("target_upside_pct" in an) == ("upside_base_price" in an)`. Quattro test su sei confermati rossi togliendo la base. 2040 backend, 412 frontend. Sette job CI verdi. Resta la verifica a schermo su DG. |
| FA-034 | P2 | Dati/UX | Le holdings di un fondo non hanno limite a nessuno dei tre strati | **DONE / PROD** | Paginate tutti e tre gli strati: la query prende `limit`/`offset` (valore di riposo 100, non «tutte» — lasciarlo aperto rimetterebbe il caso di partenza per ogni chiamante che non passa nulla), l'endpoint espone i parametri con `holdings_total`, e la tabella accumula con «Mostra altre». ⚠️ **Misurato prima di scegliere dove mettere il limite**: la dichiarazione maggiore porta 7.530 righe, e su tutte le 377.995 del magazzino le coppie (dichiarazione, ticker) duplicate sono **zero**. Conta perche la deduplicazione girava in Python DOPO la query: se togliesse righe, un `.limit()` SQL darebbe pagine corte in silenzio. ⚠️ **Ma il LEFT JOIN sul catalogo poteva moltiplicare** — il vincolo unico e su `(ticker, exchange)`, quindi lo stesso ticker su due borse e ammesso — quindi la pagina si prende **senza il join**, e il catalogo si legge dopo sui soli ticker della pagina: la difesa e strutturale invece che difensiva, perche la pagina non puo proprio moltiplicarsi. Oggi il fan-out misurato e zero, ma CLAUDE.md chiede di tenere la difesa. ⚠️ **Paginare avrebbe rotto tre aggregati** di livello-portafoglio (top 10, infografica, conteggio), che leggevano l'array intero: ora il backend manda `composition` a parte, e il caso che lo dimostra sono le uscite — hanno peso zero, stanno in fondo all'ordinamento e **nessun prefisso** le contiene, 1.301 su 7.530 nel caso maggiore. ⚠️ **Il backlog diceva che lo scarto fra i due conteggi ha «un meccanismo esatto»: e vero per 107 dichiarazioni su 145, non per tutte.** Le altre 38 vanno nella direzione OPPOSTA — fondi Dataroma dove le righe MANCANO rispetto alle dichiarate, con zero uscite, perche lo scrape ne ha prese meno. Percio' l'etichetta dice cosa conta ciascun numero e non spiega la differenza. Tre controlli negativi verificati, uno dei quali **resta verde** e lo dice: `100 < undefined` e falso comunque, quindi quel test misura il contratto e non la riga. 2098 backend, 571 frontend. |
| FA-035 | P2 | Dati | La classificazione degli strumenti e ferma a una fotografia di luglio | **DONE / PROD** | Commit `4837f3b`. ⚠️ **Il mio audit ha esagerato la portata**: diceva «ogni ETF aggiunto dopo luglio e invisibile a sette query», che suona sistemico. Misurato in produzione, il buco era UNA riga — QQQ, quotata NASDAQ e percio assente da una lista costruita per borsa. Non una deriva, ma il meccanismo l'avrebbe riaperta al prossimo fondo. Due criteri scartati e fissati come regressione: **il nome** ("Netflix" contiene "etf", lo stesso falso positivo da sottostringa di `mute` dentro `text-muted-foreground`, e me ne sono accorto perche un grep esplorativo ha restituito NFLX fra i fondi) e **la borsa**, che e esattamente il criterio che ha lasciato passare QQQ. Il criterio giusto e l'industria GREZZA, letta PRIMA che `canonical_industry` la mandi su OTHER — cosa che fa di proposito e che cancella l'unico segnale. `is_fund_industry` vive nel normalizzatore, che possiede il vocabolario. La correzione al riseed e **solo in salita**: un CSV che tace non declassa una riga gia `etf`, altrimenti le 24 righe della migration di luglio tornerebbero tutte sbagliate. Migrazione `3d8693a96ce6` sul criterio piu onesto disponibile, `industry='ETF'`, cioe la riga che dichiara cosa e. |
| FA-036 | P2 | UX/calendario | Le pastiglie del calendario non lasciano leggere il ticker | **DONE / PROD** | Commit `328e264`, sette job CI verdi. ⚠️ **Il difetto era gia stato trovato e gia corretto, con la soglia sbagliata.** Il commento in `DayCell` lo racconta: due colonne «made the tickers vanish», trentuno etichette invisibili, rimedio a `dense-3` (1400px) sul calcolo «due chip da 85px». Ma 85px non contengono un ticker, e lo screenshot che ha riaperto il caso e a **1440px**, dentro il ramo a due colonne. Seconda volta oggi con questa forma, dopo FA-027. Rifatto il conto, due colonne leggibili vorrebbero un viewport oltre i 1480px: invece di rincorrere la soglia, **una colonna sempre**. Il «+N» esiste gia ed e onesto — lo dice quello stesso commento — mentre una fila di pastiglie senza nome no. |
| FA-037 | P2 | Struttura | Due pagine di diagnostica, una delle quali si chiama Impostazioni | **DONE / PROD** | Commit `499459f`. Una destinazione `/diagnostics` con due schede, Piattaforma e Motore. ⚠️ La separazione **non e organizzativa**: disponibilita del sistema e capacita predittiva del motore sono due domande diverse, e una sorgente che non risponde e un guasto mentre un detector che legge «non concludente» non lo e — nella stessa pagina senza separarle, la seconda si leggerebbe come un allarme. Le rotte `/health` e `/settings` **reindirizzano** invece di sparire, e la scheda vive nell'URL, quindi i segnalibri reggono e un link a una singola vista resta condivisibile. Solo la scheda attiva viene montata: non e peso, e che Piattaforma tiene aperta una connessione SSE per tutta la propria vita. ⚠️ L'ingranaggio e stato **tolto**, non rinominato, e **non** sostituito da una pagina di preferenze vuota: le preferenze reali vivono ognuna accanto alla funzione che serve, e una destinazione creata per simmetria del menu sarebbe un posto dove non trovare niente. Torna quando c'e qualcosa da metterci. Sei test, confermati rossi montando entrambe le viste. |
| FA-038 | P3 | UX/navigazione | Il ritorno manca sulle due pagine di dettaglio piu visitate | **DONE / PROD** | Commit `66aa10c`. Il modello non e stato inventato: `SectorDetail` e `InstitutionalDetail` lo avevano gia, un `<Link>` verso la pagina padre col suo nome. Aggiunto a `/stocks/:ticker` (Screener) e `/markets/:symbol` (Dashboard). ⚠️ E `MacroDetail` passa da `navigate(-1)` a un `<Link>`: un ritorno alla storia del browser non porta da nessuna parte quando la pagina si apre da un link ricevuto, cioe esattamente quando serve. Il test legge il SORGENTE delle cinque pagine e cerca la destinazione: montarle vorrebbe react-query, il router e i grafici, cioe testare l'infrastruttura invece del ritorno. **Un primo tentativo costruiva un finto componente dentro il test** e avrebbe asserito che il TEST rende un'ancora: buttato, era vero di niente. |
| FA-039 | P3 | Frontend | L'AbortSignal e supportato dal client e quasi mai propagato | **DONE / PROD** | Spike S-2. Commit `2713646`, sette job CI verdi. Su 60 `queryFn` una sola lo passava; ora tutte, con una firma sola (`signal?: AbortSignal` come ultimo parametro, il precedente di `stocks.search`). ⚠️ **Misurato prima, e ridimensiona il beneficio**: il server negozia **h2** (`ALPN protocol: h2`), quindi le richieste sono multiplexate e l'argomento classico dei sei slot per host non si applica; nessun handler REST osserva la disconnessione (`is_disconnected` compare solo nei tre stream SSE), quindi un abort non ferma il backend; e react-query scarta gia i risultati delle query smontate. Resta CPU e banda del client, piu le connessioni del dev server Vite che e HTTP/1.1 — cioe igiene, esattamente il P3 che l'audit le aveva dato. **Il deliverable e il test**: `api/abortSignal.test.ts` censisce ogni `queryFn` e pretende che inoltri il segnale, con un pavimento sul censimento perche non passi su zero file. ⚠️ Un rischio introdotto e preso da `tsc`: `queryFn: fn` con riferimento nudo riceve il CONTESTO di react-query come primo argomento, che con la nuova firma finiva nello slot del segnale — cinque siti. |
| FA-040 | P3 | UX/igiene | Colonne vuote, righe mozzate, nomi che spariscono su mobile | **DONE / PROD** | Voce 2.4 del piano, chiusa dal commit `24f976d` piu lo snap sulle confluenze. (a) Su *In formazione* «Livello d'innesco» e «Distanza» mostravano un trattino su TUTTE le righe dei gruppi la cui condizione non e un attraversamento di prezzo. ⚠️ Ma un trattino non e sempre uno spreco: su una riga singola dice «questo innesco non e un attraversamento di prezzo», ed e informazione vera. La colonna sparisce solo quando NESSUNA riga del gruppo la riempie, e i test lo fissano in entrambe le direzioni. (b) In `AllocationBars` il nome era l'unica traccia flessibile, quindi su un telefono da 390px gli restavano meno di cento pixel e sui fondi spariva: restavano le percentuali senza sapere di chi. Sotto `sm` va su due righe. (c) Le righe mozzate a meta altezza nascono da una riga ad altezza fissa che non e un multiplo del passo: `snap-y snap-proximity` fa fermare lo scorrimento su righe intere, e `proximity` invece di `mandatory` perche non combatta chi scorre piano. ⚠️ **Terza volta oggi con la stessa forma**, in tre componenti non imparentati: cede sempre l'ETICHETTA e sopravvive la decorazione. Registrato in CLAUDE.md con la regola e le tre soluzioni. |
| FA-041 | P2 | UX/screener | Il Divario fra le due lenti esiste solo per i settori | **DONE / PROD** | Voce 4.2 del piano, audit §7.3. Commit `e0df074`, sette job CI verdi sul run 34606029076. Lo screener aveva `composite` e `tech_composite` sulla STESSA riga da sempre e non ne faceva la differenza. **Ordinabile lato server** come `pct_off_high`: `DIVARIO_EXPR` vive nel SORT e ordina tutte le 925 righe con entrambe le lenti — ordinare lato client le 50 righe della pagina le presenterebbe come una classifica dell'universo, che e il difetto della colonna market cap di FA-026. Le righe con una lente sola hanno Divario NULL e finiscono in fondo in ENTRAMBI i versi: sconosciuto non e zero, e zero e per giunta il valore che significa «le due coincidono». ⚠️ **Misurato prima di decidere, e la misura ha cambiato il progetto**: su 925 titoli `|Divario|` ha p50 17,9 e p90 44,2, e il titolo mediano legge **-13, non 0** — le due lenti sono entrambe su 0-100 ma non centrate allo stesso modo, quindi un Divario nullo non vuol dire «le due concordano», e la frase sta nel titolo della colonna. Percio' lo screener **non marca le righe notevoli**: la soglia degli 8 punti dei settori marcherebbe il 77,2% dei titoli, e qualunque costante scritta nel codice invecchia in silenzio mentre i punteggi si muovono (la trappola `_RANGE_PERIODS`). A trovare le code ci pensa l'ordinamento, che vede tutte le righe. ⚠️ **Niente rosa/smeraldo**: in questo progetto quella tavolozza significa direzione di mercato e la colonna Δ% accanto la usa per una direzione vera, mentre +20 e -20 qui sono due oggetti diversi e non «buono» e «cattivo» — e lo studio score-IC nega che il composito Qualita preveda i rendimenti. Proprietario unico dell'aritmetica e del segno: `lib/lensGap.ts`, con `sectorLens` che delega. |
| FA-042 | P2 | UX/setup | La trimestrale dentro la finestra di attesa di un setup | **DONE / PROD** | Voce 4.3 del piano, audit §7.4. Commit `ac1d7c6`, sette job CI verdi, pod `Running ready=true` sull'immagine corrispondente. *In formazione* diceva da quanto un setup aspetta e il calendario quando il ticker pubblica; separate, nessuna delle due poteva chiedere se **il setup si risolve prima o dopo la trimestrale** — e una trimestrale sovrascrive la tesi tecnica. ⚠️ **Misurato prima di decidere, e la misura ha cambiato due cose.** Su 60 setup in lista l'11 settembre, 43 con data in cache: la trimestrale piu vicina e a **11 giorni**, la mediana a 55, con due grappoli (40-60 = stagione Q3, 150-195 = chi ha appena riportato). Quindi oggi il marcatore **non si accende su nessuna riga, e quella e la risposta giusta**: lo zero e una fotografia del calendario, non una proprieta della funzione — l'errore speculare a FA-041, dove una soglia si accendeva sul 77% delle righe e per questo non marcava niente. Seconda conseguenza: la finestra e il **tetto** da `first_seen_at` e non la scadenza scorrevole da `last_seen_at`, che si sposta a ogni scansione ed e permanentemente a dieci giorni da oggi (misurato: scorrevole p50 9g max 10g, tetto p50 14g max 27g, tetto vincolante su 9 righe su 60) — il `min()` sembra prudente e SOTTO-RIPORTEREBBE il caso per cui la funzione esiste. Consolidati due proprietari unici, `stock_fundamentals_service.next_earnings_dates_cached` e `lib/earningsProximity.ts`: la regola esisteva gia due volte e un terzo consumatore avrebbe fatto tre copie. ⚠️ Due test si sono rivelati **vuoti** e il controllo negativo li ha scoperti: quello sull'arrotondamento passava anche con `Math.floor` perche a est di Greenwich e in CI (UTC) i due coincidono — forzato `TZ=America/New_York` ha trovato un difetto vero, `-0`; e uno chiedeva via HTTP uno stato che lo schema vieta. Sette controlli negativi verificati. 2068 backend, 515 frontend. |
| FA-043 | P2 | UX/diagnostica | Una sorgente degradata non lo dice dove il dato si consuma | **DONE / PROD** | Voce 4.4 del piano, audit §7.5. Commit `a39cdc4`, sette job CI verdi, immagine servita in produzione prima del rollout di `2970cb2`. Salute sapeva che Marketaux era fuori servizio; la scheda News mostrava solo meno articoli, quindi il degrado era visibile a chi andava a cercarlo. ⚠️ La catena autorizzata e **fonte -> tipo di dato -> scheda e si ferma li**: quanto un degrado costi a UN titolo non lo sa nessuno, e un test lo pinna cercando «articoli» e «meno» nel testo reso. ⚠️ **Il motivo dell'errore non esce dall'endpoint**: `last_failure_reason` e grezzo e non redatto, e fra quelli misurati c'e una URL che puo portare un token in query string — il contratto ha quattro campi e nessuno dove un token possa finire. ⚠️ **`unavailable` non e un degrado** e l'app lo aveva gia deciso: significa plan-gated (tutti HTTP 403) e `DataSourcesCard` lo dipinge slate con la nota «non un incidente»; includerlo avrebbe dato due significati allo stesso stato su due schermi, e in pratica una riga permanente (`finnhub/upgrades`: 0 successi su 1272). ⚠️ **La prima misura era falsa**: `kubectl exec python -` legge i contatori da un processo nuovo e riporta 19 fonti su 19 `idle`, perche `data_source_metrics` vive in memoria e non ha un `hydrate`. Interrogato il worker via HTTP dall'interno del pod: `overall: outage`, `marketaux/news` failing (429, quota 56/100), `yfinance/fundamentals` e `yfinance/live_quote` failing per rate limit. **Seguito dichiarato e non fatto**: le due fonti piu consumate dell'app sono degradate adesso e restano mute — il meccanismo e generale (`<SourceDegradedNote op=… />`) ma il piano nomina la scheda News. Quattro controlli negativi verificati. 2077 backend, 522 frontend. |
| FA-044 | P3 | UX/navigazione | Nove destinazioni in un elenco piatto | **DONE / PROD** | Voce 5.2 del piano, l'ultima. Commit `2970cb2` (era `422bfb9` prima del rebase), sette job CI verdi, pod `Running ready=true restarts=0` sull'immagine corrispondente. Esplora, Screener, Calendario e Superinvestor rispondono tutte a «che cosa c'e la fuori»; In formazione, Segnali e Posizioni a «che cosa sto seguendo». ⚠️ Sono **etichette**, non pagine: nessuna rotta cambia. `/sectors` che si chiama «Esplora» resta un difetto di leggibilita degli indirizzi, e rinominarlo romperebbe i segnalibri senza risolvere niente che si veda — il piano lo esclude dalla stessa tranche. ⚠️ **Due gruppi invece di tre**: «Strumenti» avrebbe contenuto Stato e Metodo, che FA-037 ha gia fuso in una destinazione sola, quindi avrebbe avuto un figlio unico — una riga di cornice con zero informazione. Un test pinna che nessun gruppo etichettato possa avere meno di due voci. ⚠️ **L'ordine di Monitoraggio e cambiato**: era Segnali prima di In formazione, cioe la barra raccontava la storia al contrario, mentre da FA-029 e FA-042 la catena setup -> segnale -> posizione e percorribile nei dati. `NAV` e **derivato** dai gruppi e non riscritto, perche il titolo della scheda si legge da li e due liste a mano divergerebbero al primo inserimento. Quattro controlli negativi verificati, incluso quello che conta: una destinazione persa nel raggruppamento, che e' il momento esatto in cui una voce sparisce mentre la barra continua a sembrare piena. 529 frontend. |
| FA-045 | P1 | UX/valuta | Il dialogo di un segnale stampa `$` su ogni prezzo | **DONE / PROD** | ⚠️ Misurato in produzione: **2.669 segnali su 8.905, il 30,0%**, sono su titoli non quotati in dollari (1.084 GBP, 1.012 EUR, 545 HKD, piu una coda DKK/NOK/AUD) — stessa magnitudine di FA-025, che era P1. Il payload dell'alert non portava la valuta affatto, quindi il dato mancava alla radice: `Stock.currency` viaggia ora nelle DUE query, e lo spacchettamento e in un punto solo perche `_row_to_item` fu estratto apposta. Viaggia **grezza**, `GBp` compreso: normalizzare l'etichetta e compito di `displayCurrency`, che lo fa gia per ogni prezzo dell'app, e due normalizzazioni divergono. Quattro superfici cablate al proprietario unico `lib/money.formatMoney`: prezzo trigger e invalidazione nel dialogo, il feed dei segnali recenti, e le quattro celle del piano operativo — dove il formattatore locale `price()` e stato **eliminato**, non affiancato. ⚠️ `PlaybookView` prende `currency` come prop OBBLIGATORIA e non opzionale con un default: il default sarebbe il dollaro, cioe il difetto che la prop esiste per chiudere. Due controlli negativi verificati. 2089 backend, 566 frontend.
| FA-018 | P2 | Accessibilita | Aggiungere descrizione accessibile al dialog degli alert prezzo | **DONE / PROD** | `PriceAlertDialog` espone `DialogDescription` screen-reader-only; warning Radix corretto, codice deployato e suite frontend verde. |
| FA-019 | P0 | Release | Push e sincronizzazione cloud dei commit locali | **DONE / PROD** | Push verificato; CI 34366785799 completata con tutti i job verdi, Argo `Synced/Healthy`, StatefulSet sull’immagine `42693fb1b09195be96065cfdc1bda0be176c014a`, health esterno 200 e `/metrics` esterno 403. |
| FA-021 | P2 | GitOps | Eliminare lo stato Argo `OutOfSync` del Cluster CNPG quando il diff effettivo è vuoto | **DONE / PROD** | Verificato live 2026-09-09: `postgres-cluster` torna `Synced/Healthy` e tutte e cinque le Application sono sincronizzate; il cluster resta sano 1/1 e nessun pod si e riavviato. Causa isolata: l'operatore aggiunge `enabled: true` alla voce di `spec.plugins`, e il CRD non dichiara `x-kubernetes-list-type`, quindi la lista è ATOMICA e una chiave in più rende diversa tutta la lista. `postgresql.parameters` non causa drift benché l'operatore vi inietti 23 chiavi: è una mappa e ArgoCD possiede solo le sue sette. Rimedio: dichiarare il campo nel manifest, **non** un `ignoreDifferences` su `/spec/plugins`, che silenzierebbe anche una modifica vera all'archiviatore. |
| FA-046 | P2 | UX/mobile | Otto traboccamenti orizzontali a 375px, e 112 spiegazioni che su un telefono non si aprono | **DONE** | Commit `979455b`. ⚠️ **Il controllo ovvio riporta zero e la risposta e falsa**: `html`/`body` sono `overflow-x: clip` e `<main>` ha `overflow-y: auto`, che per specifica forza `overflow-x` ad `auto` — quindi nessuna barra compare a livello di documento e un rilevatore che salta i contenitori scorrevoli scarta l'INTERA applicazione, perche `<main>` e la radice di tutto. Il primo rilevatore ha detto «zero offensori» su ogni rotta; il controllo negativo l'ha mancato perche la sonda era appesa a `document.body`, cioe fuori da `<main>`. Rimisurato dal riquadro di `<main>`: /institutionals +201px, /stocks +87, /sectors/:name +84, /macro/:id +66, /calendar +51, /institutionals/:slug +15, /setups +13, /alerts +9. ⚠️ Nessuno era una larghezza scritta a mano: `flex-wrap` sul contenitore **non spezza un elemento** piu largo della riga (su /calendar e /stocks il wrap c'era gia e non serviva a niente), e `truncate` su un flex item **non entra in funzione senza `min-w-0`** — `SectionTitle`, il titolo di ogni scheda dell'app, si piantava sulla propria min-content a 375px dentro un genitore di 350 mentre il commento accanto dichiarava chiuso un difetto della stessa famiglia. Un `<select>` si dimensiona sull'opzione piu lunga e ignora il genitore. **Le spiegazioni**: 112 attributi `title`, quasi tutti su intestazioni di tabella, che su touch non si aprono MAI — il difetto e di accesso, non di spazio, e per questo era invisibile su desktop. `ui/info-hint.tsx` le apre al tocco e all'hover; Radix Tooltip sarebbe stato lo stesso difetto con un'altra libreria (si chiude su `pointerdown`, cioe sul tap), e il click deve APRIRE e non commutare o col mouse `pointerenter` ha gia aperto e il toggle richiude subito. 18 intestazioni convertite, piu la nota metodologica dei setup (303 caratteri, 120px su mobile) e la didascalia 13F. ⚠️ **Le altre didascalie /setups NON sono state toccate**: portano bande di confidenza e «non concludente», cioe dati — dietro un tocco mostrerebbero un tasso senza il campione, che e la regola di onesta che il repo applica altrove. Il censimento in `mobileLayout.test.ts` ha trovato subito **nove intestazioni che il grep a mano aveva mancato**. Verificato dopo: 13 rotte a 375px e 4 a 1440px, zero offensori. 590 frontend. |
| FA-047 | P1 | Verifica | Otto presidi per il divario fra «codice corretto» e «codice che ha girato» | **DONE** | Il divario piu' grande del progetto, con quattro istanze documentate: unit k3s, `_RANGE_PERIODS` morto, livello `apt` inerte 24 giorni, otto traboccamenti mobile invisibili a 590 test verdi. **(1) Gate UI Playwright** su tre viewport e dieci rotte, nella catena di `image`: chiude una classe invisibile PER COSTRUZIONE, perche' jsdom non fa layout. ⚠️ Ha trovato `/calendar` che sborda il giorno stesso, due volte (19px poi 14px: `flex-wrap` va messo a OGNI livello, non solo in cima). **(2) Notturno** trivy sull'immagine DISPIEGATA — scansionare una ricostruzione risponderebbe alla domanda sbagliata. **(3) Provenienza** scritta nell'artefatto dentro lo stesso `RUN` delle patch: asserzione positiva, non lettura del log di buildkit. **(4) CronJob di parita'** dentro il cluster, perche' ArgoCD e' pull-based e CI non raggiunge il nodo; allarma solo oltre 20 minuti, che e' la finestra normale del GitOps. **(5) Codice mai eseguito**: 351 funzioni su 1.366, cricchetto sul delta — non una percentuale di copertura, che si Goodhart-izza. **(6) Censimento istogrammi**: generalizza il p95=1.000, ogni serie nuova va dichiarata col p99 plausibile. **(7) Mutazione** su quattro moduli a proprietario unico; ⚠️ `mutmut` scartato perche' NON gira su Windows e spedire un cancello mai visto girare e' il difetto stesso. Passata completa: 130 mutanti, 43 uccisi, 80 sopravvissuti unici (punteggio 33%), congelati come linea di base col cricchetto. Al primo giro quattro sopravvissuti, **due lacune vere su righe COPERTE** — il bordo esclusivo della soglia e il taglio di una data con orario. **(8) Deriva del nodo**: confronta dichiarato ed eseguito, incluso «l'unit e' piu' recente del processo»; eseguito sul nodo vero, ALLINEATO. **Cruscotto**: scheda «Verifica» coi due arretrati, col DENOMINATORE accanto e la RAGIONE in un popup — un arretrato nudo diventa un obiettivo da azzerare, e azzerarlo sarebbe sbagliato. ⚠️ **Due lezioni generali**: una linea di base derivata da una misura va generata DOVE viene applicata (costato due giri di CI su due presidi diversi), e il pavimento di contenuto ha salvato l'intero gate — in CI ogni rotta rendeva 0 caratteri e senza di esso dieci asserzioni sarebbero passate misurando il nulla. 601 frontend, 2.123 backend. |
| FA-048 | P1 | Verifica | La mutazione da 4 a 12 moduli, e il cancello che arrossiva su un commento | **DONE** | Commit `a67fcae`..`HEAD`. **451 mutanti, 240 uccisi (53%)**; erano 130 e 43 (33%) su quattro moduli. ⚠️ Il denominatore SCENDE da 465 a 451 perche' togliere una duplicazione in `technical_score_service` ha tolto 14 mutanti: meno codice non verificato, non piu' test. **Tre difetti veri, nessuno trovato leggendo il codice.** (1) `app/core/security.py` uccideva ZERO dei suoi nove: `except ValueError: return False` -> `True` significa che un hash bcrypt MALFORMATO autentica chiunque, e `isinstance(u, str) and 0 < len(u) <= 64` -> `or` accetta uno username vuoto o da duecento caratteri e fa esplodere `len()` su un non-stringa (rifiuto pulito -> 500). Chiuso a 7 su 9. (2) Un titolo FERMO spariva dalla lente Tecnico: l'RSI di una serie piatta e' tutto NaN, `.iloc[-1]` su una serie vuota solleva IndexError e il `except Exception` di `partial_for` rende None — mentre la guardia che sembrava coprirlo (`if n > 15`) era codice MORTO, perche' `partial_for` sbarra a 30 barre. La riga del MACD due sotto lo faceva gia' bene (`if hd.size`). (3) La costruzione della riga persistita era duplicata fra `finalize` e `recompute_one` — duplicazione che aveva GIA' prodotto un 500 in produzione — e la sonda l'ha resa visibile segnalando gli STESSI mutanti due volte: **mutanti in coppia sono un indizio di duplicazione, non di doppio lavoro**. ⚠️ **Il cancello era rotto**: chiave `file:riga`, quindi aggiungere un COMMENTO a un modulo sorvegliato spostava ogni voce sotto e la notturna (senza `continue-on-error`) arrossiva su codice intatto — misurato, nove righe di commento -> quattro falsi sopravvissuti, un riassetto -> 85. Ora `modulo::funzione#N  prima -> dopo`, con quattro test piu' un CONTROLLO NEGATIVO che fissa l'instabilita' della vecchia forma, e un pavimento che pretende che ogni EQUIVALENTE corrisponda a un mutante che ESISTE (una voce orfana non protegge niente e resta li' a sembrare una spiegazione). ⚠️ **E un elenco di test troppo CORTO in `BERSAGLI` produce FALSI sopravvissuti**: `base.py` riportava 42 su 58 con due file elencati mentre i test veri sono 45 in `tests/signals/`, e 18 dei 63 sopravvissuti della vecchia linea di base (~29%) erano falsi. Un conteggio alto di sopravvissuti SI LEGGE COME RIGORE, ed e' la ragione per cui l'errore non si denuncia da solo. ⚠️ **Un residuo alto non vuol dire la stessa cosa in due moduli**: il magazzino esiti chiude a 47/57 perche' e' fatto di CONTRATTI, la lente Tecnico a 39/93 perche' e' fatta di TARATURE, e fissare una taratura rende rossa ogni ricalibrazione legittima. Il criterio del triage non e' «quanto e' importante» ma «cosa succede a chi ritara». |
| FA-049 | P1 | Presidi | Il CronJob di parita' immagine leggeva il pod sbagliato e allarmava sempre | **DONE** | Commit `2938707`. La quinta istanza di «una risposta pulita e falsa da uno strumento guasto», e la piu' istruttiva: il presidio funzionava da mesi e si e' rotto senza che nessuno toccasse il suo codice. Leggeva `kubectl get pod -l app.kubernetes.io/name=finance-alert -o jsonpath='{.items[0]...}'` e **quell'etichetta ce l'ha OGNI pod del chart**, compresi i Job che il chart stesso crea — il drill di ripristino e i pod della sonda medesima. `.items[0]` prende il primo in ordine ALFABETICO, e `drill-...` precede `finance-alert-...` perche' d < f. E' bastato eseguire un drill a mano: il suo pod e' rimasto `Completed`, la sonda ha letto la SUA immagine (`alpine/k8s:1.31.3`) e per sette ore ha ripetuto ogni quindici minuti «il pod NON esegue l'immagine desiderata da 409 min» — 409 essendo l'eta' del pod del DRILL — mentre l'applicazione era sana e in pari. ⚠️ **Il danno peggiore non e' l'avviso sbagliato**: (1) un allarme che suona sempre insegna a ignorarlo, la stessa fatica da allarme per cui la pastiglia «in ritardo» e' passata da 1 a 4 giorni; (2) ArgoCD riportava `Degraded` in permanenza, perche' un Job fallito degrada l'intera Application — cioe' il presidio aveva reso cieco il cruscotto che serve a vedere i degradi VERI. **La regola: un pod di StatefulSet si NOMINA, non si cerca** — `<sts>-<ordinale>` e' garantito dalla specifica e nessun Job puo' chiamarsi cosi'; dove un selettore serve davvero deve FALLIRE se trova piu' di una corrispondenza invece di prendere la prima. Aggiunta anche la guardia sulla lettura vuota: senza, due stringhe vuote si confrontano uguali e la sonda dichiara «parita' OK» mentre il pod non esiste, lo stesso difetto col segno invertito. Verificato sul cluster VIVO prima di spedire: col comando nuovo desiderato == in esecuzione == il commit spinto, pronto true. |
| FA-050 | P1 | Verifica | La mutazione su dodici moduli: 33% -> 66%, e otto accoppiamenti non dichiarati | **DONE** | **445 mutanti, 294 uccisi (66%)**; erano 130 e 43 (33%) su quattro moduli. Per modulo: `signal_outcome_service` 84% (era 35), `fx_service` 81, `core/security` 78 (era **ZERO**), `detectors/base` 74 (era 36), `confluence_service` 74 (era 45), `detector_performance` 72 (era 38), `setup_service` 62, `technical_score` 42 (era 14, residuo di TARATURA). ⚠️ **Il filo di tutti i rilievi: la sonda non trova bug, trova ACCOPPIAMENTI NON DICHIARATI.** In nessuno dei casi il codice era sbagliato quel giorno; era sbagliato il contratto. (1) Quattordici periodi di indicatore cablati su cinque file perche' la «fonte unica» viveva dentro un servizio che importa SQLAlchemy, quindi `signals/context.py` — calcolo puro — non poteva importarla: NON era importabile da chi ne aveva bisogno, quindi nessuno la importava, quindi ognuno la riscriveva. Ora e' un modulo foglia. (2) La costruzione della riga Tecnico duplicata fra `finalize` e `recompute_one`, duplicazione che aveva GIA' prodotto un 500 in produzione. (3) Due mediane in `setup_service`, e quella annidata NON ordinava: funzionava perche' il chiamante ordinava venti righe sopra — su un elenco non ordinato rispondevano 30 contro 7. (4) Un titolo FERMO spariva dalla lente Tecnico (RSI di serie piatta tutto NaN) mentre la guardia che sembrava coprirlo era codice morto. (5) Un hash bcrypt MALFORMATO autenticava chiunque. ⚠️ **Il corollario: quando un mutante sopravvive la domanda non e' «quale test manca» ma «quale invariante sto dando per scontata».** Quattro risposte alla prima domanda hanno prodotto test; quattro alla seconda hanno TOLTO la condizione. ⚠️ E **due mutanti sulla stessa riga vanno decisi separatamente** — successo quattro volte: su `if x <= 0 or ref <= 0` il termine su x e' equivalente e quello su ref rende 1.0, il fattore piu' forte possibile, proprio quando la scala non esiste. E' la ragione dell'ordinale `#N` nella chiave. **Cinque famiglie di sopravvissuto NON sono lacune**: il termine `x <= 0` dove zero rende zero, i bordi di una curva CONTINUA (che sono la prova che non ha salti — si asserisce la continuita', non si uccide il mutante), guardie irraggiungibili, `return` difensivi, `frozen=True` senza consumatore. |

## Percorso segnali: piano operativo (2026-09-14)

Origine: un audit esterno sul ciclo **setup → segnale → esito** (ridondanze,
presentazione degli esiti, correlazione fra titoli, dettaglio titolo).
**Riverificato voce per voce sul database di produzione** il 2026-09-14 prima
di impegnare qualunque lavoro: undici affermazioni su dodici reggono, e tre
sono più gravi di come erano descritte. I numeri in FA-051..FA-066 sono quelli
rimisurati, non quelli dell'audit.

⚠️ **Un audit di un altro strumento va trattato come lo strumento di cui non si
conosce il guasto.** Due cose che la riverifica ha corretto: l'audit girava
sull'immagine `2938707`, cinque commit indietro rispetto alla produzione, e
leggeva il working tree mentre `indicators/periods.py` non era ancora
committato. Le sue letture del DATABASE restano valide — i dati non cambiano
con l'immagine — quelle del CODICE andavano rifatte.

⚠️ **Il COME sta in un documento a parte**, non qui: [implementation-plan-2026-09-14-percorso-segnali.md](implementation-plan-2026-09-14-percorso-segnali.md) porta, voce per voce, i file e le funzioni toccate, i test da scrivere, i rischi e cosa NON fare. Questo file resta la lista operativa; quello non ne apre una seconda.

### L'ordine di esecuzione NON è l'ordine valore/costo, ed è una misura a deciderlo

L'audit metteva il drift per primo e l'identità degli alert per terza. **È
l'inverso**, per un motivo che si vede solo misurando insieme le due cose:

```
candle_reversal, allarme "decaying" acceso oggi
   con duplicati    n=1372   37,2%   base 49,0%   IC [34,7 – 39,8]  → ALLARME
   senza duplicati  n= 866   46,1%   base 49,0%   IC [42,8 – 49,4]  → nessun allarme
```

Il più rumoroso dei tre allarmi di drift **è fabbricato dalla duplicazione**.
Correggere il drift per primo lo zittirebbe per la ragione sbagliata: diventerebbe
«evidenza insufficiente» — onesto — nascondendo che il campione è per un quarto
ri-letture dello stesso evento. La deduplica è la diagnosi, il dimensionamento
è l'igiene, e vanno in quest'ordine.

E la duplicazione è **l'unico difetto che peggiora ogni giorno**: 193 righe
eccedenti nel solo settembre. Ogni giorno di ritardo aggiunge righe che FA-052
dovrà riconciliare.

### Le fasi

| Fase | Voci | Perché qui |
|---|---|---|
| **A — fermare ciò che peggiora** | FA-051, FA-052 | L'identità dell'evento. Prima si ferma, poi si riconcilia lo storico: cancellare per somiglianza romperebbe riferimenti a esiti, setup e posizioni. |
| **B — ciò che A nascondeva** | FA-053 | Numerosità effettiva del drift, con lo stato «evidenza insufficiente». Solo dopo A, altrimenti si misura il rumore di A. |
| **C — perimetri e serializzazione** | FA-054, FA-055, FA-056 | Tre modi diversi di mostrare una popolazione e descriverne un'altra. FA-054 e FA-055 si compongono: riparare uno solo non fa comparire l'esito. |
| **D — affermazioni predittive residue** | FA-057, FA-058, FA-059 | Costo basso, e nessuno richiede dati nuovi: si tolgono affermazioni, non se ne aggiungono. |
| **E — significati** | FA-060, FA-061, FA-062 | «Esito», «orizzonte», «prezzo». FA-061 richiede una migrazione ed è la sola voce con perdita storica dichiarata. |
| **F — contesto e consolidamento** | FA-063, FA-064, FA-065, FA-066 | FA-066 dipende da C ed E: una UI migliore su una storia instabile è lavoro da rifare. |

### Cosa NON si spedisce, e cosa manca per poterlo fare

Queste **non** sono lavoro rimandato: sono affermazioni che i dati attuali non
reggono. La stessa distinzione che FA-006 e FA-007 portano già in questo file.

| Proposta | Cosa manca prima di poterla presentare |
|---|---|
| Probabilità che un setup converta entro una finestra | FA-061 (episodi conservati) + storia della visibilità + copertura delle scansioni, poi validazione temporale fuori campione |
| «Questo detector funziona meglio su questo titolo/settore» | Numerosità indipendente sufficiente e correzione per confronti multipli. Il cubo descrive il campione, non giustifica la raccomandazione |
| Vantaggio da co-temporalità, famiglia o multi-orizzonte | Studio preregistrato nuovo, finestre non sovrapposte, fuori campione. **I sei risultati nulli restano il riferimento** |
| Rendimento del piano stop/target come risultato realizzabile | Regole di esecuzione, gap, costi, dati intrabar, e il trattamento delle barre che toccano entrambi i livelli |

### Invarianti del piano

Tre cose che nessuna voce deve violare, e che l'audit stesso raccomandava:

1. **Le formule di Forza e Probabilità non si toccano.** Nessuna voce qui
   cambia un punteggio; cambiano identità, perimetri ed etichette.
2. **Le tre tabelle non si fondono.** `StockSetup`, `Alert` e `SignalOutcome`
   registrano fatti diversi — un'attesa, un evento, una misura. Il problema è
   come si collegano e si aggiornano, non che siano tre.
3. **La scheda setup rimossa dal dettaglio titolo non torna.** FA-066 evolve la
   scheda storica esistente; non aggiunge una seconda lista.

## Cosa resta aperto (2026-09-11)

**La Tranche 1 del piano e chiusa.** I quattro difetti di significato dei numeri
sono corretti: FA-030 (la finestra 52 settimane era l'intera storia su cinque
timeframe su sei), FA-031 (il macro sbagliava la scala di mille volte),
FA-032 (una variazione di quote stampata con l'unita del peso, con tre valori
sopra il massimo aritmeticamente possibile) e FA-033 (quattro prezzi base per
lo stesso titolo). Le prime tre sono in produzione e verificate; FA-033 e verde su
tutti e sette i job e in attesa del solo rollout.

Ognuna ha portato un test che e stato **confermato rosso** togliendo la
correzione, che era la condizione d'ingresso posta dal piano — e non era una
formalita: il blocco 52W non aveva alcuna asserzione, ed e per questo che il
difetto e sopravvissuto a un rename restando invisibile per mesi.

**Le cinque TRANCHE del piano sono chiuse.** La 4 (le connessioni fra oggetti)
con FA-029 posizione-segnale, FA-041 il Divario nello screener, FA-042 la
trimestrale nella finestra del setup e FA-043 la sorgente degradata; la 5 con
FA-037 le due diagnostiche e FA-044 il raggruppamento della navigazione.

⚠️ **«Le tranche sono chiuse» non e «il piano e chiuso», e la differenza non e
una sottigliezza.** Il piano contiene anche quattro spike e una decisione, e
soprattutto pone cinque criteri di chiusura di cui il QUINTO e la verifica a
schermo, che spetta all'utente — la divisione del lavoro e scritta in testa al
documento: jsdom non calcola gli stili, quindi contrasto, focus visibile e
target touch non sono misurabili dai test, e nessuna quantita di CI verde li
sostituisce. Delle sei voci chiuse fra il 10 e l'11 settembre, **zero hanno il
quinto criterio soddisfatto**.

Questa nota esiste perche l'affermazione «il piano e completo» e stata fatta e
poi corretta nella stessa sessione. E la forma gia registrata qui due volte —
una correzione parziale che avvalora la convinzione che il caso sia chiuso — e
qui il commento accanto al codice sarebbe stato il backlog stesso.

Restano quindi, oltre alle verifiche a schermo: **D-1 / FA-026**, che aspetta
una decisione e non lavoro; gli spike **S-1** (riprodurre la barra indici
vuota), **S-2** (propagare l'`AbortSignal`) e **S-3** (i finding ESLint non
gated); e **S-4**, bloccato da fuori — il kubelet local-path non espone
`volume_stats`, quindi serve una sorgente diversa, non lavoro.

⚠️ **Le prime due voci della Tranche 4 sono rimaste committate e non pushate
per una sessione intera**, e il working tree pulito lo nascondeva: «interrotto»
non voleva dire codice a meta, voleva dire lavoro finito e testato mai uscito
dalla macchina. Le due prove che lo dicono senza ambiguita sono il **reflog** —
ogni commit precedente e seguito da `rebase (start): checkout origin/cloud`, gli
ultimi due no — e l'**immagine sul pod**, ferma al commit precedente. Nessuna
delle due e il `git status`. Pubblicate il 2026-09-11 con il run 34606029076,
sette job verdi.

⚠️ Due volte in questa tranche il mio audit ha **esagerato la portata** di un
difetto che poi era reale ma piccolo: FA-035 non era una deriva ma una riga, e
il ritorno mancava su due pagine e non su sei. Il meccanismo era vero in
entrambi i casi — quindi valeva la pena chiuderlo — ma la misura andava fatta
prima di scrivere «ogni», non dopo.

⚠️ **Due dei tre difetti erano gia stati trovati e gia corretti, con la soglia
sbagliata.** FA-027 risolveva sotto `sm` una colonna che e stretta da `lg` in
su; FA-036 spostava a 1400px un calcolo che ne vuole 1480. In entrambi i casi
il commento accanto al codice descriveva il difetto e dichiarava di averlo
chiuso, il che e' precisamente cio che lo ha tenuto nascosto: una correzione
parziale avvalora la convinzione che il caso sia risolto. **Quando un commento
dice di aver sistemato qualcosa, rifare il conto costa meno che fidarsi.**

Le **quattro** voci qui sotto sono quello che resta, e solo UNA e lavoro: due
in attesa di dati (FA-006, FA-007), una bloccata da fuori (FA-015), e una da
fare (FA-008).

FA-028 e chiusa senza lavoro, ed e l'esito corretto: lo spike chiedeva di
riprodurre prima di ipotizzare, e la riproduzione ha mostrato che la striscia
si comporta allo stesso modo sui tre viewport. Il difetto stava nell'ordine
degli scatti, non nella pagina.

⚠️ **Il conteggio va rifatto, non copiato.** Questa frase ha gia detto «otto»
mentre la tabella sotto ne elencava undici: un numero plausibile accanto a una
tabella che lo smentisce viene creduto, perche nessuno lo riconta.

FA-011 e FA-013 non aspettano piu l'utente: collaudate su browser vero l'11
settembre, e il collaudo ha trovato tre difetti che i test non potevano vedere
— il drawer che si comporta da modale senza dichiararlo, la data del calendario
scritta in UTC e riletta in locale, e il 404 senza titolo di scheda.

FA-026 non aspetta piu una decisione: e stata presa l'11 settembre (opzione C)
e la voce e chiusa. FA-039 e chiusa dallo spike S-2.

⚠️ **Il conteggio va rifatto, non copiato.** Questa frase ha detto «otto» mentre
la tabella sotto ne elencava undici, ed e la stessa forma del conteggio ESLint
in CLAUDE.md: un numero plausibile accanto a una tabella che lo smentisce viene
creduto, perche nessuno lo riconta.

**Le tre voci residue del piano sono state chiuse l'11 settembre** e hanno
preso un ID qui — FA-042, FA-043, FA-044 — perche il file lo richiede a ogni
attivita e nascere dal piano invece che dall'audit numerato non e una
deroga: senza ID il Divario era rimasto fuori dal backlog canonico per un
giorno intero.

⚠️ **La verifica a schermo ha trovato due difetti che i test non potevano
vedere**, ed e la giustificazione del quinto criterio di chiusura in forma
concreta. Il primo era mio, introdotto lo stesso giorno: in FA-044
`Diagnostica` e fuori da ogni gruppo nel DOM — il test lo asserisce e passa —
ma resa senza stacco sotto `Posizioni` si LEGGE come l'ultima voce di
Monitoraggio. Struttura giusta, lettura sbagliata; corretto con un separatore
e ora pinnato da un test sul confine. Il secondo e FA-045, che e piu grande di
cosi e resta aperto.

⚠️ **CHIUSO il 2026-09-13, e la promessa era sbagliata su meta.** Il seguito
di FA-043 prometteva la nota su `fundamentals` E `live_quote`, «due righe per
scheda, nessuna nuova decisione». La decisione c'era, ed e stata presa
diversamente per i due:

- **`fundamentals` cablato**, in ENTRAMBI i rami di `FundamentalsCard`. Quello
  vuoto conta piu' dell'altro: dice «dati non disponibili per questo ticker» e
  basta, cioe un'assenza INSPIEGATA, mentre yfinance e l'unica fonte e non ha
  ripiego. Col dato presente serve lo stesso, perche un degrado non produce una
  tabella vuota ma una tabella VECCHIA servita dalla cache L2, che da una
  fresca non si distingue.
- **`live_quote` NON cablato, di proposito.** `StockHeader` e
  `MarketStateBadge` mostrano gia `market_state === "STALE"`: due meccanismi per
  lo stesso significato sulla stessa schermata sono il difetto delle due
  tavolozze in un'altra forma, e qui potrebbero anche CONTRADDIRSI — `STALE` e
  per-quotazione e vivo, la nota e per-fonte e cachata due minuti. Un test
  protegge l'omissione dal prossimo «completamento» E verifica che lo stato
  per-quotazione esista ancora, perche se sparisse la ragione cadrebbe.

⚠️ **E la premessa era scaduta.** Il backlog diceva `fundamentals` e
`live_quote` «failing adesso»; rimisurato sul worker vivo il 13 settembre,
l'unica fonte degradata e `marketaux/news` — gia coperta. Il lavoro resta
giusto (quelle fonti degradano davvero, sotto rate limiting yfinance e
misurata a 43-50s), ma non e stato verificato guardando la produzione.

⚠️ Il controllo che mancava: `known_ops()` documenta da mesi che «un refuso nel
frontend renderebbe una scheda muta per sempre e nessuno lo vedrebbe mai» —
l'endpoint risponde 422, ma la nota non ritenta e non avvisa in caso di errore,
deliberatamente. Le due scelte sono giuste separatamente e insieme producono il
silenzio. `tests/test_source_degraded_ops.py` censisce ogni `op` montato nel
frontend e lo confronta col catalogo; vive nel BACKEND e legge i sorgenti del
frontend, perche duplicare l'elenco in TypeScript creerebbe la seconda copia
che il controllo esiste per impedire.

Sequenza, criteri di chiusura e verifiche richieste all'utente:
[implementation-plan-2026-09-10.md](implementation-plan-2026-09-10.md).
⚠️ Una sola voce e bloccata da una scelta e non da lavoro: **FA-026**, dove
convertire il market cap in USD, etichettarlo per valuta o fare entrambe le
cose cambia numeri gia visibili a schermo. Il piano raccomanda la terza.

Le voci da FA-027 in poi vengono dall'audit unificato
([frontend-audit-2026-09-10.md](frontend-audit-2026-09-10.md)), che fonde la
revisione Claude e quella GPT Astra 6 sulla stessa acquisizione di 426
screenshot.

⚠️ **Correzione.** La revisione Claude apriva con "il desktop e il viewport
peggiore dell'app", sostenuta da "35 contenitori con scorrimento interno sulla
dashboard desktop". Erano **scatti**, non contenitori: la cattura campiona
inizio/centro/fondo di ogni contenitore. Contati correttamente sono 9 su
desktop e 4 su mobile, e il totale su 16 pagine e desktop 35 / tablet 31 /
**mobile 39**. La tesi generale e falsa: il desktop e il peggiore su 2 pagine,
il mobile su 4. Sopravvive solo il rilievo stretto, che resta utile — sulle due
pagine piu dense il desktop annida da due a sette volte piu del mobile.

**Il risultato dell'audit unificato e un altro:** i difetti gravi non sono di
layout ma di **significato dei numeri**. Sei valori a schermo sono sbagliati o
ambigui in modo che orienta una decisione, e tre lo sono di molto — un fattore
1000, un fattore 100 e un minimo del 1932 etichettato come minimo annuale.

| ID | Cosa serve per chiuderla | Chi puo sbloccarla |
|---|---|---|
| FA-006 | un giorno di borsa sull'immagine `8cd929a`, poi rimisurare `/api/stocks/quotes` | il tempo |
| FA-007 | sessioni browser reali che popolino i percentili RUM | il tempo |
| FA-008 | ~~classificare e correggere 47 finding ESLint non gated~~ chiusa il 2026-09-13, ricontata a zero il 2026-09-15 | — |
| ~~FA-015~~ | chiusa il 2026-09-15: le `volume_stats` c'erano, la regex non le trovava, e su local-path descrivono il nodo | — |
| FA-051 | P0 | Segnali | L'identita' dell'evento: la deduplica smette di funzionare appena l'esito matura | **DONE / PROD** | **Verificata in produzione** sull'immagine `dc1263e`, con `GIT_SHA` letto DA DENTRO il container. Commit `0c8ab3c`. ⚠️ **Il difetto non era il congelamento, era cosa significa congelare**: la guardia «freeze post-esito» e' giusta e resta — l'Esito descrive la barra e il prezzo di allora — ma uscendo dall'INTERA condizione, «non modificare» diventava «inserisci», che e' il contrario di congelare. Ora una ri-lettura sulla stessa barra non fa niente; una barra diversa crea ancora la sua riga. Due test, uno dei quali riproduce la forma reale del difetto (tre passate di scansione -> una riga; prima erano quattro). ⚠️ **Verificato che la correzione non fosse PARZIALE**, che e' la trappola che CLAUDE.md registra: l'identita' e' «stessa data», e un detector di stato timbra la data sull'ultima barra, quindi poteva esistere un secondo canale a date ravvicinate. Misurato: **82 coppie di date distinte entro il cooldown in tutto il magazzino, contro 668 righe stessa-data** — e CPRX ha 39 righe su sole **5 date distinte**, cioe' 34 su 39 le toglie questa correzione. Le date distinte NON sono lo stesso difetto: una candela d'inversione sei giorni dopo, su una barra diversa, e' un evento diverso, e che meriti la sua riga e' una decisione gia' presa che un test esistente fissa. **Misure originarie**: 668 righe eccedenti su 9.034 alert (7,4% del magazzino), 596 identiche al centesimo**, in 170 gruppi (stock, detector, data, tono). Il meccanismo e' in `signal_scan_service.py:218`: la guardia «freeze post-esito» esce dall'INTERO blocco quando `_outcome_matured` e' vero, quindi cade nel ramo «nuovo alert» e conia una riga a ogni scansione. ⚠️ **La data del primo duplicato e' la prova**: CPRX `candle_reversal` scatta il 2026-07-09 e il primo duplicato compare il 2026-07-16 — cinque sedute dopo, cioe' il giorno della maturazione. 35 righe, tutte a 31,49 esatti, fino a **8 in un solo giorno** (una per scansione, non una per giorno: il tasso dipende da quanto spesso si scansiona, non dal mercato). ⚠️ E i responsabili sono **solo due detector, `candle_reversal` (506) e `gap_and_go` (162)** — gli unici due a orizzonte 5 giorni, gli unici che maturano DENTRO la propria finestra di recency. E' una previsione che il meccanismo fa e che i dati confermano, non una correlazione trovata a posteriori. Vivo: 240 righe eccedenti a luglio, 235 ad agosto, 193 a settembre. **Chiusura**: ri-rilevare lo stesso evento (stessa data, stesso tono) dopo la maturazione non crea niente; una data diversa crea ancora. Prova di idempotenza sulla sequenza rilevazione -> maturazione -> nuova scansione. |
| FA-052 | P1 | Segnali | Riconciliare le righe duplicate storiche senza cancellarle alla cieca | **DONE / PROD** | **Eseguita il 2026-09-14**, commit `947fd3a`. Backup PRIMA di cancellare e del MEDESIMO insieme cancellato — le righe eccedenti dei gruppi bonificabili, non «i duplicati» in generale: un backup di un insieme diverso da quello cancellato non e' un backup. 666 righe, riletto e verificato; 504 `candle_reversal` + 162 `gap_and_go` = 666, cioe' i 668 eccedenti meno i 2 del gruppo SDR.L bloccato. **Verificato contro la PREVISIONE**: alert 9039 -> 8373 (-666), esiti 5314 -> 4648 (-666, il CASCADE ha agito esattamente), posizioni con alert 2 -> 2 e setup convertiti 306 -> 306 (**il `SET NULL` non e' mai scattato**), esiti orfani 0, gruppi residui 1. ⚠️ E la verifica che chiude il cerchio causale di FA-053: `candle_reversal` passa da **37,2% a 46,0%** contro una base di 49,0% — previsto 46,1% PRIMA di cancellare, e lo scarto e' l'unico esito maturato nel frattempo. `gap_and_go` si muove nella direzione OPPOSTA (52,9% -> 47,4%), che e' la firma di una selezione e non del rumore. **Resta SDR.L** `candle_reversal` 2026-07-06, intatto: l'originaria porta un esito disallineato (FA-067). **Strumento e misure originarie**: Commit `3dacdb9`: rilevatore `alert_service.find_duplicate_alert_groups` + `app.scripts.repair_duplicate_alerts`, sola lettura per default come i due script di bonifica accanto. ⚠️ **La bonifica NON e' stata eseguita**: la passata distruttiva aspetta il rilascio e un via libera esplicito. Misurato prima di progettare: delle 668 eccedenti **tutte hanno un esito, tutte sono archiviate, ZERO portano una posizione e ZERO chiudono un setup** — quindi il pericolo `SET NULL` non si materializza su QUESTI dati, ma il rilevatore lo sorveglia lo stesso, perche' il prossimo insieme di dati non e' questo. Tre blocchi che impediscono la bonifica automatica di un gruppo: una posizione collegata, un setup convertito, o un esito dell'originaria disallineato (vedi FA-067). Nove test, compreso un pavimento — un magazzino pulito non rende gruppi — senza il quale ogni asserzione negativa sarebbe vera di niente. **Criterio di chiusura originario**: dipende da FA-051 (prima si ferma, poi si riconcilia). Le 596 righe non si cancellano automaticamente per somiglianza: portano `signal_outcomes`, possono portare il `converted_alert_id` di un setup e riferimenti di posizione. Serve una mappa di equivalenza che elegga la riga originaria (la prima per `triggered_at`) e sposti i riferimenti, con conteggio prima/dopo. **Chiusura**: nessun riferimento orfano, e il conteggio del magazzino esiti cala della quantita' prevista dalla mappa, non di piu'. |
| FA-053 | P0 | Misura | Il monitor di drift dimensiona su righe, non su finestre indipendenti | **DONE / PROD** | **Verificata in produzione** sull'immagine `dc1263e`: il monitor legge ora **0 segnalati, 11 evidenza insufficiente**, e la ragione e' visibile — `macd_divergence` ha 170 righe ma **2 finestre**, con intervallo [21,4-97,9], e un intervallo che va dal 21% al 98% non puo' escludere niente. Prima la stessa riga leggeva [71,4-83,8] e gridava «in salita». Commit `a46f1ae`. ⚠️ **Il danno vero non era l'allarme sbagliato**: tutto cio' che NON veniva segnalato rendeva `direction="stable"`, cioe' l'assenza di prova si presentava come prova di stabilita'. Gli stati sono ora quattro e `insufficient` non e' una quarta sfumatura di stabile — significa che il campione non puo' rispondere. ⚠️ **La collocazione, stessa lezione di `app/indicators/periods.py`**: `wilson_interval` viveva nel drift e `independent_blocks`/`sized_interval` nel cubo, **che importava il primo dal secondo**, quindi il drift non poteva importare le finestre senza chiudere un ciclo. Non poteva, quindi non lo faceva, quindi misurava peggio. Ora in `app/stats/sizing.py`, modulo foglia, con un test che ne legge l'AST e pretende che resti tale. ⚠️ **I cinque test preesistenti che cadevano non segnalavano una regressione**: codificavano il dimensionamento vecchio, e uno codificava letteralmente il danno (`direction == "stable"` su sei righe). Tutte le fixture seminavano gli alert sulla STESSA data — quaranta titoli che scattano lo stesso giorno sono un giorno di mercato visto quaranta volte, cioe' UNA finestra — quindi le date sono state distanziate invece che le asserzioni indebolite. Il chip del frontend mostrava `(n 1372)` col conteggio delle righe: ora mostra `effective_n`, col titolo che nomina orizzonte e finestre. **Misure originarie**: tre allarmi accesi su undici detector, e **tutti e tre sono falsi, per due ragioni diverse**. (1) `candle_reversal` «decaying»: 37,2% su n=1372 contro base 49,0%, IC [34,7-39,8] -> allarme. Togliendo le righe duplicate di FA-051: **46,1% su n=866, IC [42,8-49,4], la base rientra, l'allarme sparisce**. La causa e' una selezione, non un caso: una candela di inversione ri-rilevata per due settimane e' per costruzione una che NON ha invertito, quindi i duplicati sovra-campionano i fallimenti. (2) `macd_divergence` e `hidden_divergence` non hanno duplicati ma poggiano su **2 finestre indipendenti** ciascuno, contate col criterio che il cubo usa gia' (`independent_blocks`). ⚠️ **E c'e' un'aritmetica da dichiarare, non da aggirare**: con finestra di 90 giorni e `min_n=30`, il tetto delle finestre indipendenti e' **12** a orizzonte 5 giorni e **3** a 21. Nessun detector potra' mai superare quella soglia. La correzione giusta non e' abbassare `min_n` finche' qualcosa passa, e' dire che un monitor di drift a 90 giorni non ha potenza statistica a questi orizzonti. Serve un terzo stato — stabile / drift supportato / **evidenza insufficiente** — e l'assenza di allarme non deve leggersi come prova di stabilita'. **Chiusura**: parita' del criterio di numerosita' col cubo, confronto prima/dopo, e i tre allarmi che oggi suonano o spariscono o diventano inconcludenti con la ragione scritta. |
| FA-054 | P1 | Segnali | Lo storico del titolo esclude gli archiviati: **1 esito visibile su 5.313** | **DONE / PROD** | **Verificata in produzione** sull'immagine `8f147b1`, sui DATI VERI e non solo sul tag: su `LMP.L` la vista «Recenti» rende 2 alert e **0 con esito**, lo storico completo ne rende 16 con **14 esiti**, e 16 = 2 + 14. I quattordici esiti maturati di quel titolo erano TUTTI su alert archiviati. La riga campione legge `valuta=GBP`, cioe' il caso che il codice vecchio avrebbe reso con `$`. ⚠️ Due correzioni sono servite dopo il primo tentativo, entrambe trovate dal gate UI: (1) Radix `TabsTrigger` emetteva `aria-controls` verso un `TabsContent` mai reso — ora due bottoni con `aria-pressed`, e axe ne segnalava UNO su sette perche' tollera un riferimento pendente con `aria-expanded="false"`; (2) avevo fatto variare il TITOLO con la vista e il gate e2e, che localizza la scheda per quel testo, non la trovava piu' — il nome di una scheda e' la sua identita' e non cambia con la vista. Commit `9dc03d3`: schede «Recenti / Storico completo». I recenti restano i non archiviati — scelta di prodotto, la scheda non ha una colonna Archivio — e lo storico e' `/api/alerts`, che gia' pagina e gia' porta gli esiti. ⚠️ **Ma quella rotta non sapeva esprimere «entrambi»**: `archived` rende `False` quando il parametro e' assente, quindi da una query string `None` e' irraggiungibile, e la UI offriva solo «attivi» o «solo archiviati». Aggiunto `include_archived`, additivo. ⚠️ **E la striscia di statistiche NON compare sullo storico**: e' calcolata sulle righe CARICATE, quindi su una pagina da 25 accanto a un totale di 400 direbbe «rialzisti 12» intendendo un'altra cosa. Quattro test, con la sonda eseguita. ⚠️ Uno dei quattro asseriva che il finto fosse richiamato e falliva: react-query serve la pagina 0 dalla cache, quindi si asserisce la PAGINA MOSTRATA — legare il test al numero di chiamate lo legherebbe alla politica di caching. **Note precedenti**: ⚠️ **Meta' fatta con `dc1263e` (FA-055), meta' aperta**, e la distinzione non va arrotondata: il payload porta ora esito e valuta, ma i RECENTI restano i non archiviati — che e' una scelta di prodotto, la scheda non ha una colonna Archivio — quindi quei 5.312 esiti **non si vedono ancora**. Manca la scheda «Storico completo» che punti a `/api/alerts?ticker=…&archived=`, con paginazione su `total`. La rotta esiste gia' e non ne serve una nuova. **Difetto originario**: `stock_detail_service.py:281` filtra `archived_at IS NULL` e limita a 50. Misurato sul vivo: 5.313 esiti maturati, **5.312 su alert archiviati**, quindi la scheda «storico» del dettaglio titolo ne mostra **uno**. E' la sesta istanza della regola gia' scritta in CLAUDE.md — *mai filtrare una misura su un campo che scrive l'UTENTE* — dopo le tre chiuse il 2026-09-05 (cubo, curva equity, drift): archiviazione e maturazione seguono entrambe l'ETA', quindi la loro intersezione e' quasi vuota. Esempio: CPRX ha 41 alert complessivi e 2 non archiviati. **Chiusura**: Recenti / Storico completo nello stesso spazio, conteggi sul dataset intero, paginazione per non appesantire il dettaglio. |
| FA-055 | P1 | API | `AlertOut` ricostruito a mano in `stocks.py`, senza esito ne' valuta | **DONE / PROD** | **Verificata in produzione** sull'immagine `dc1263e`, con `GIT_SHA` letto DA DENTRO il container. Commit `dc1263e`. Il dettaglio titolo prende ora gli alert da `alert_service.list_alerts`, che gia' fa la LEFT JOIN sul magazzino esiti, porta la valuta di quotazione e legge le trimestrali dalla cache; l'ampiezza resta calcolata sugli oggetti ORM con una query puntuale sugli id della pagina. ⚠️ **Il filtro e' per `stock_id`, non per ticker**: il catalogo e' deduplicato su (ticker, exchange), quindi due borse possono condividere un simbolo. ⚠️ **E i sei campi non SPARIVANO dal payload**: `AlertOut` e' Pydantic con default, quindi venivano serializzati come `null` — le chiavi c'erano tutte. E' peggio, non meglio, perche' `outcome_hit: null` non distingue «non ancora maturato» da «questa rotta non lo popola», ed e' la ragione per cui un test sulle CHIAVI passa anche col difetto presente: resta nel file dichiarato per quello che e', una guardia sullo schema. Il tipo `Alert` del frontend dichiarava gia' `currency` e `outcome_hit` e non li riceveva mai: ora arrivano senza toccare una riga di TypeScript. **Difetto originario**: `api/stocks.py:454` costruisce l'oggetto campo per campo invece di usare la serializzazione canonica di `schemas/alert.py`, e ne omette **sei**: `currency`, `outcome_hit`, `outcome_fwd_return`, `outcome_horizon_days`, `outcome_mkt_excess`, `next_earnings_date`. Si compone con FA-054: anche riparando il filtro archiviati, l'esito non arriverebbe comunque. ⚠️ `currency` mancante significa prezzi senza valuta su un terzo dell'universo (312 titoli su 1010 non sono in dollari) — il difetto che `lib/money.ts` ha gia' chiuso altrove. **Chiusura**: un solo proprietario della serializzazione; aprire lo stesso alert da qualunque pagina rende gli stessi campi. |
| FA-056 | P1 | Setup | Perimetro della lista setup: paginazione, filtri e ordinamento lato server | **DONE / PROD** | **Verificata in produzione** sull'immagine `c52f9fd`, `GIT_SHA` letto da dentro il container. `api/setups.py` rende al massimo 50 righe senza paginazione, e `useSetups.ts` non chiede le successive. Misurato: **1.415 setup attivi**, 59 in shortlist, la lista ne rende 50. Sui chiusi la divergenza e' maggiore: **637 chiusi nel database, lista limitata a 50, statistiche calcolate su 18**. ⚠️ Il filtro `shortlisted` delle statistiche **non e' un difetto**: `conversion_stats` lo applica con la ragione scritta accanto — *un setup che l'utente non ha mai visto non gli ha fatto nessuna promessa* — ed e' difendibile. Il difetto e' che la pagina mostra una popolazione e ne descrive un'altra senza dirlo. Inoltre ordinamento e filtro detector operano sul sottoinsieme gia' ricevuto. **Chiusura**: totale filtrato nel payload, perimetro statistico dichiarato a schermo, ordinamenti che vedono tutte le righe. |
| FA-057 | P1 | UX | La «convinzione» multi-orizzonte, e il tie-break rialzista che la esprime | **DONE / PROD** | **In produzione** sull'immagine `8f147b1`. Commit `cadd9d9`. La chiave di ordinamento e' estratta in `_chiave_ordinamento` e il test la confronta DIRETTAMENTE su due gruppi speculari, invece di asserire chi viene prima: un test sull'ordine di due elementi pari dipende dalla stabilita' del sort, cioe' da un dettaglio dell'algoritmo. Un altro test pretende che gli orizzonti restino nel payload — senza, «togliere il premio» scivolerebbe in «togliere l'informazione». **Difetto originario**: `AlertsInsightCard.tsx:176` rende un'icona con `aria-label="Convinzione: multi-orizzonte rialzista"`, e `confluence_service.py:222` ordina a parita' di Forza privilegiando `multi_horizon and direction == "bull"`. ⚠️ **Non e' senza fondamento**: il codice cita lo studio del 2026-06-09, che una deriva direzionale rialzista l'ha misurata. L'argomento e' un altro, ed e' interno: **il trade playbook ha cancellato la parola `conviction` il 2026-09-02 per questa identica ragione** — «il piano descrive una geometria, non impartisce un'istruzione» — e qui e' sopravvissuta. Un'incoerenza verificabile, non una questione di gusto. **Chiusura**: gli orizzonti restano descrittivi, il trattamento preferenziale e la parola vanno via; reintrodurre una preferenza predittiva richiede risultati fuori campione nuovi. |
| FA-058 | P2 | UX | Ripetizioni nel riepilogo confluenze: Top long/short accanto alla Top 10, Forza media | **DONE / PROD** | **In produzione** sull'immagine `8f147b1`. Commit `cadd9d9`. Con loro se ne vanno `ExtremeCell`, `avgStrength` e l'import `Gauge`, diventati morti. **Difetto originario**: `AlertsInsightCard.tsx:375-376` rende Top long e Top short **sopra** una graduatoria che li contiene gia', occupando la prima fascia con elementi ripetuti; riga 424 rende una «Forza media» aggregata su confluenze di composizione eterogenea, senza un impiego dichiarato. **Chiusura**: una sola graduatoria filtrabile per direzione; la media sparisce dal riepilogo operativo. |
| FA-059 | P1 | UX | «Movimento del titolo, non una condizione di mercato» afferma una causa | **DONE / PROD** | **In produzione** sull'immagine `8f147b1`. Commit `cadd9d9`. Ora: «Nessun altro titolo del catalogo scansionato ha mostrato questa condizione quel giorno». ⚠️ Il test esistente si chiamava «calls a lone signal a move of the stock, which is the useful reading» e asseriva la frase vecchia: **codificava l'affermazione da togliere**. Riscritto con controllo negativo su entrambe le meta' della frase vecchia — senza, riscriverla all'indietro passerebbe. **Difetto originario**: `SignalBreadthRow.tsx:66`: quando nessun altro titolo ha lo stesso detector nello stesso giorno, la scheda conclude che il movimento e' del titolo. **L'assenza di altri match non dimostra la causa del movimento**, e il perimetro coperto non e' il mercato: e' il catalogo scansionato quel giorno. La riga accanto (il caso non-solitario) e' invece scritta bene e dichiara gia' che la coincidenza non e' una conferma. **Chiusura**: «nessun altro match osservato nel perimetro coperto», e il perimetro nominato. |
| FA-060 | P1 | Misura | «Esito», «orizzonte» e «prezzo» nominano cose diverse nelle diverse pagine | **DONE / PROD** | **In produzione** sull'immagine `a7175fd`, `GIT_SHA` letto da dentro il container. Commit `a7175fd`. Tre correzioni. (1) **Orizzonte**: il playbook diceva «Orizzonte» per la CAMPATA DELLA CATENA mentre l'Esito accanto e' misurato sulla finestra del DETECTOR — ora «Tenuta attesa», col controllo negativo eseguito. ⚠️ Misurato: **dieci detector su sedici** portano piu' di un'etichetta per la stessa finestra; `sr_flip` ne ha tre su 807 righe tutte a 21 sedute, e `analyst_momentum` legge «short» mentre e' misurato a 63. (2) **Verdetto**: 491 su 4.648 (10,6%) divergono fra assoluto e market-neutral — due domande, non un errore — ma il tooltip diceva «ha battuto la MEDIA», e il valore e' una MEDIANA. ⚠️ La causa a monte e' un nome: la colonna si chiama `universe_mean_fwd` e contiene una mediana; il commento accanto spiega perche' NON va rinominata (~24 riferimenti, sei script di studio), quindi si corregge cio' che l'utente legge. (3) **Prezzo**: 2.246 su 4.648 (48%) hanno un ingresso diverso dal prezzo dell'alert; `outcome_entry_close` viaggia ora nel payload e il dialogo mostra «Ingresso misurato» QUANDO differisce. ⚠️ Buco dichiarato: la cella nuova non ha un test di componente — il dialogo non ha imbracatura e montarlo richiede impalcatura. Il campo e' coperto dal backend (presenza e assenza), la resa condizionale no. Tre divergenze misurate sul vivo. (1) **596 esiti su 5.313** hanno verdetto diverso fra direzione assoluta e confronto market-neutral: non e' un errore, rispondono a due domande, ma portano la stessa etichetta. (2) L'etichetta d'orizzonte a schermo viene dalla CAMPATA della catena (per segnale), la misura dal detector: `squeeze_expansion` mostra «Breve» su 815 righe misurate a 21 sedute, e **`sr_flip` porta tre etichette diverse — short 407, long 252, medium 148 — per la stessa identica misura a 21**. (3) **2.886 esiti su 5.313** hanno un prezzo d'ingresso diverso dal prezzo dell'alert, perche' l'aggiornamento in cooldown sposta il prezzo mentre la misura resta ancorata alla barra del segnale. **Chiusura**: etichette distinte per conversione del setup, variazione del titolo, risultato nella direzione indicata ed eccesso sulla mediana; durata della struttura separata dalla finestra di misurazione; prezzo/data della misura mostrati accanto a prezzo/data della rilevazione. Non uniformare misure legittimamente diverse. |
| FA-061 | P2 | Segnali | Episodi di setup: un record per episodio invece di riusare la riga | **DONE / PROD** | **In produzione** sull'immagine `ebfcc2c`, e verificata DENTRO il container: `alembic_version = 78d64d497ae0`, `closed_reason` presente, e l'indice legge `CREATE UNIQUE INDEX ... WHERE ((status)::text = 'active'::text)` — con la sua clausola, che e' la meta' che conta. Commit `c664a12`, `107af3c`, `ebfcc2c`. ⚠️ **Tre dei quattro rilievi di questa voce erano fuori dal suo perimetro**, e nessuno si vedeva leggendo il codice: le ragioni di chiusura erano gia' calcolate e buttate via; un `try/except` attorno a una `DROP CONSTRAINT` avrebbe abortito la transazione su Postgres al primo rilascio; le migrazioni non erano MAI girate su Postgres; e la catena storica non e' eseguibile da zero (FA-070). Si sono viste accendendo un presidio, non ragionando. **Meta' UI CHIUSA il 2026-09-14**, commit `8e6b035` (tono) e `a17bbf1` (ragione di chiusura). ⚠️ **Il tono era un difetto piu' grave di come la voce lo descriveva**: il commento sopra `SqueezeExpansion.proximity` diceva testualmente «this setup carries NO tone ... saying "bull" here would be inventing a forecast, which is exactly what setups must not do» e tre righe sotto il codice scriveva `tone="bull" if ctx.trend_sign >= 0 else "bear"` — il difetto `_RANGE_PERIODS` nella sua forma piu' pura. In produzione: i suoi setup leggevano `bull` **487** volte contro `bear` **180**, ed e' l'UNICO detector che converte in un alert di tono diverso (63 su 230, il 27%). La tabella delle intestazioni della UI lo corroborava gia': `squeeze_expansion:bull` e `:bear` erano **byte-identiche**. ⚠️ E la suite non lo sorvegliava: cambiando il tono, **2.463 test sono passati senza accorgersene**. ⚠️ La trappola evitata: il frontend scriveva `const bull = tone === "bull"` in DUE componenti — con quel booleano un terzo tono non produce un terzo caso, produce «ribassista», cioe' la direzione sbagliata con la stessa sicurezza di prima. **Ragione di chiusura**: la misura ha CAMBIATO il progetto invece di confermarlo — **322 scaduti senza ragione contro UNO con la ragione**, perche' la colonna e' nata quel giorno, quindi sostituire «Scaduto» con la ragione precisa avrebbe reso una schermata che legge «ragione non registrata» 322 volte. La pastiglia resta uno STATO; la ragione sta nella riga sotto; solo «Ritirato» — che sta FUORI dal denominatore del tasso di conversione — si prende una pastiglia propria. ⚠️ E l'argomento che avevo in mente non regge e non e' nel codice: il tasso «a occhio» e' 48,6% contro 48,7%, un decimo di punto come FA-067. A giustificare il lavoro e' la TRAIETTORIA, non la magnitudine: li' la popolazione era chiusa e in calo, qui cresce da zero. Restano fuori, con la ragione scritta: gli episodi chiusi conservano il tono e la ragione con cui furono registrati — **la storia non si riscrive** — e gli attivi passano a `undetermined` alla prossima scansione, da soli. **Difetto originario**: `setup_service.py:140` riattiva la STESSA riga azzerando `resolved_at`, `converted_alert_id` e `lead_days`; riga 117 la cancella se la convenienza scende sotto soglia. Il vincolo `UniqueConstraint(stock_id, detector)` rende impossibile conservare piu' di un episodio. Quindi lo storico dei setup non e' uno storico: e' l'ultimo episodio di ogni coppia. ⚠️ **Gli episodi gia' sovrascritti non sono ricostruibili** e vanno dichiarati mancanti, non stimati. Serve anche una direzione «non determinata»: tutti i 63 collegamenti con tono setup diverso dal tono alert sono `squeeze_expansion`, che dichiara esplicitamente di non conoscere ancora il verso. **Chiusura**: unicita' limitata all'episodio APERTO, motivi di chiusura distinti (condizioni decadute / attesa massima / dati insufficienti / cambio di direzione), storia della visibilita' in shortlist separata dal flag corrente. Richiede migrazione. |
| FA-062 | P2 | Segnali | Osservabilita' di aggiornamenti, freschezza e mancata maturazione | **DONE** | Commit `92df901` (stati di misura + provenienza) e `b4d4c85` (perimetro). ⚠️ **Due dei quattro stati che questa voce nominava NON sono stati costruiti, perche' la misura li ha smentiti**, ed e' la parte che conta. Misurato in produzione il 2026-09-14 su 3.734 alert senza esito: **3.686 in attesa dell'orizzonte, 48 BLOCCATI su 12 titoli, e ZERO che avrebbero dovuto maturare**. Lo stato «avrebbe dovuto maturare» ha popolazione vuota come FA-067; «non valutabile» ne ha **UNA** (un solo alert senza `signal_name` in tutto il catalogo). Costruire un'etichetta per una riga sarebbe sembrare di aver corretto qualcosa. ⚠️ **La misura originaria di questa voce era sbagliata e l'ho smentita io**: i «7 alert oltre 120 giorni» venivano da una soglia in giorni di CALENDARIO, mentre i detector a 63 sedute sono ~88 giorni — a 73 giorni stanno legittimamente aspettando. La soglia giusta conta le BARRE dopo il segnale. **Lo stato «bloccato» e' verificato, non assunto**: `ohlcv_nodata_streak >= 3` seleziona esattamente i 12 titoli la cui ultima barra ha piu' di dieci giorni — zero falsi positivi, zero falsi negativi — e interrogata, la fonte conferma (nove senza barre; per WBS, EQR e AVB l'ultima barra disponibile e' AL GIORNO la nostra, con AAPL come controllo positivo nella stessa chiamata). ⚠️ NON si riusa `not_quarantined_clause()`: porta il termine su REPROBE_DAYS e risponde a un'altra domanda, quindi l'alert oscillerebbe fra i due stati ogni sette giorni a dati fermi. **Il catalogo lo sapeva dal 2026-08-26** — quella data chiuse il lato FETCH (smettere di scaricare i morti) e nessuno chiese cosa significasse per gli alert appesi a quei titoli. **Provenienza**: 6.674 alert su 8.396 portano un `amend_count`, con punte di **371 revisioni**, e **2.807 VISIBILI** hanno una data d'origine diversa da quella a schermo; l'unico posto dove la data originaria compariva era un attributo `title`, invisibile su un dispositivo tattile. **Perimetro**: il proprietario comune esisteva dal 2026-09-02 ma non prendeva scopo; ora `universe` e' obbligatorio e senza default — un default a True darebbe il comportamento pericoloso a chi si dimentica. Difetto latente (37 scansioni parziali, tutte di maggio, 0 negli ultimi dieci giorni) ma FA-061 ne ha alzato il prezzo: una chiusura sbagliata ora e' permanente e finisce nel denominatore del tasso di conversione. **Difetto originario**:  L'aggiornamento in cooldown sposta data, prezzo e snapshot prima della maturazione, quindi «evento originario» e «ultima osservazione» sono lo stesso campo. «In maturazione» copre anche cio' che non sta aspettando: **7 alert senza esito hanno signal_date oltre 120 giorni**, alcuni aggiornati di recente. Inoltre lo scan del singolo titolo in `api/alerts.py` non attraversa il completamento dello scan generale e invalida solo il dettaglio titolo. **Chiusura**: stati di misura espliciti (in attesa dell'orizzonte / bloccato per dati mancanti / non valutabile / maturato), un proprietario comune delle operazioni successive che distingua perimetro locale e globale. |
| FA-063 | P2 | Grafico | I marker dei segnali: mezzanotte UTC e colore per maggioranza | **DONE / PROD** | **In produzione** sull'immagine `ebfcc2c`. `signalMarkers.ts:101` converte la data giornaliera del segnale in `Date.parse(day)` — mezzanotte **UTC** — e sceglie l'ultima barra con tempo <= t. Su un grafico intraday quella mezzanotte precede la prima barra della seduta, quindi il marker scivola nella seduta PRECEDENTE. Una data giornaliera non contiene l'ora di emissione e il codice non deve fingere che la contenga. Inoltre il colore deriva dalla maggioranza bull/bear delle righe sulla barra: due segnali correlati rialzisti coprono un ribassista, che e' una logica diversa dalla de-correlazione per famiglia che `confluence_service` applica altrove. **Chiusura**: collocazione giornaliera esplicita, simbolo misto con conteggi separati quando coesistono i due versi, dimensioni uniformi e nessun premio visivo alla Forza. |
| FA-064 | P2 | Segnali | L'ampiezza conta i titoli senza distinguere la direzione | **DONE / PROD** | Commit `372b9ae`. ⚠️ **La misura in produzione e' molto piu' larga dei 557 gruppi della voce**: su 8.397 alert con data, il conteggio di **7.088 (84,4%)** includeva titoli scattati al contrario, con mediana 6 e punte di 93; **2.910 dei 3.740 alert visibili**. LAND.L leggeva 20: sono 15 nella stessa direzione e 5 opposti. Il conteggio opposto non viene buttato — che lo stesso detector sia scattato al contrario su N titoli si dice in una frase sua, anche quando il titolo e' solo nel proprio verso — il settore conta i soli compagni nello stesso verso, e un compagno senza tono non e' ne' stesso ne' opposto. I campi API cambiano nome col significato (`same_day_same_tone`, `same_day_opposite_tone`), cosi' un bundle in cache legge `undefined` invece di un numero che vuol dire altro. ⚠️ **Il «collegamento ai componenti» NON e' un link a `/alerts` filtrato**, che era la scorciatoia ovvia e sarebbe stata sbagliata: quel filtro sulle date legge `triggered_at`, l'ampiezza `signal_date`, e 2.807 alert visibili li hanno diversi. `GET /api/alerts/{id}/peers` applica lo stesso predicato, archiviati compresi, e un test su tutti i bordi pretende che lista e numero coincidano — sono equivalenti per lettura, non per costruzione, e il docstring lo dice. Verificato in sola lettura su Postgres di produzione su cinque alert reali. **Non fatto, e detto**: le percentuali della voce, perche' nessuna percentuale e' a schermo — non c'era un denominatore da proteggere; e il raggruppamento per FAMIGLIA, perche' il numero dichiara «stesso detector» e fonderlo per famiglia cambierebbe cosa misura senza una misura che lo chieda. |
| FA-065 | P2 | Segnali | `build_context` costruito due volte per ogni titolo di ogni scansione | **DONE / PROD** | Commit `5603352`. `detect_signals_and_setups` accetta `ctx` e lo costruisce solo se manca; `evaluate_signals` passa quello gia' costruito per i propri cancelli. **Parita' numerica verificata** come la voce chiedeva: su cinque passeggiate aleatorie seminate da 320 barre il risultato col contesto passato e' identico a quello costruito dal runner, confrontato sul `repr` di `asdict` perche' `nan == nan` e' falso — con un pavimento che pretende almeno un rilevamento, altrimenti due liste vuote sarebbero uguali. Un test conta le costruzioni sostituendo `build_context` in ENTRAMBI i moduli che lo importano per nome: sostituirlo solo nel modulo d'origine non conterebbe niente. ⚠️ **Nessun guadagno prestazionale dichiarato**, come la voce imponeva: non c'e' profilazione. **Non fatto, e detto**: le mappe parallele di etichette e di ripiego degli orizzonti citate a margine restano dove sono — sono un'altra duplicazione e non cambiano cosa la scansione calcola. |
| FA-068 | P1 | Verifica | Il seme non copre `/institutionals`, quindi il gate misura quello che capita | **DONE / PROD** | Commit `95e7afe`, gate UI verde in CI sul run di `db2ed97` e seme nell'immagine servita. ⚠️ **Il meccanismo era piu' preciso di «misura quello che capita»**: `_catch_up_institutionals_on_boot` lancia Dataroma e SEC 13F VERI quando `filings_refresh_is_stale` risponde vero, e risponde vero anche quando NON ESISTE nessun filing. Il database del gate nasceva vuoto, quindi ogni corsa avviava gli scraper reali in un thread mentre Playwright misurava: e' il `0 -> 2` fra due corse a sei minuti. Sette fondi con nomi lunghi e tipi misti, due filing ciascuno; le azioni (new/add/reduce/sold_out) le calcola `compute_qoq_deltas`, cioe' il codice di produzione, e nessun valore consuma il `rng` del catalogo. ⚠️ **Le date dei filing erano la quarta dipendenza dall'orologio del seme**: la pagina marca «stale» oltre 183 giorni, e ho sbagliato l'aritmetica nel docstring prima di misurare — e' UN trimestre indietro a stare a cavallo della soglia (135-228 giorni), non due. Un test scorre 367 giorni di «oggi» e pretende lo stesso stato per ogni fondo, col controllo negativo che fissa l'instabilita' di un trimestre solo. Pavimenti: le tre regioni devono SCORRERE (backend e un test Playwright che nomina quelle che non scorrono) e il pavimento di caratteri passa da 150 — la pagina vuota — a 8.000 contro 10.226 misurati; un contratto pretende che dopo la semina l'avvio non lanci gli scraper. ⚠️ Cinque posizioni per filing invece di dodici NON arrossano: il pavimento misura il contenuto aggregato di sette fondi, e il controllo giusto era ridurre i fondi. |
| FA-067 | P2 | Misura | Diciotto esiti descrivono una barra che il loro alert non mostra piu' | **DONE** | **Chiusa dalla MISURA, senza scrivere codice**, ed e' la parte che conta. Rimisurata dopo la bonifica: **18 righe su 4.648 (0,39%)**, tutte `candle_reversal`, con scarto da 8 a 28 giorni. Tre numeri la chiudono insieme: (a) per mese di maturazione — giugno 16, luglio 2, **agosto 0, settembre 0** — quindi il canale e' chiuso; (b) **ZERO sono ancora visibili**, tutte archiviate; (c) escluderle sposta il tasso di `candle_reversal` da **48,7% a 48,8%**, un decimo di punto. ⚠️ Scrivere il filtro significherebbe aggiungere una join su due percorsi caldi per non cambiare niente, e poi SEMBRARE di aver corretto qualcosa — che e' peggio di non averlo fatto. E la guardia che chiude il canale non e' una convinzione: `test_matured_prior_is_frozen_and_new_row_inserted` asserisce che `signal_date` resti congelato dopo la maturazione, ed e' verde. **Cosa la riaprirebbe**: un conteggio che torna a crescere, cioe' qualcuno che tocca quella guardia. Emersa leggendo uno per uno i dieci esiti divergenti di FA-052, invece di contarli. Prima che esistesse la guardia «freeze post-esito», un aggiornamento in cooldown poteva spostare `signal_date` in avanti DOPO la maturazione: l'esito resta internamente coerente — zero righe hanno `matured_at` prima della propria `signal_date` — ma **l'alert si e' spostato via da lui**, quindi la colonna Esito descrive una barra che la riga non mostra. Misurato: **18 righe su 5.313**, per mese di maturazione 16 a giugno, 2 a luglio, **0 ad agosto e 0 a settembre**. La guardia ha chiuso il canale e il residuo e' storia. Lo scarto va da 8 a 28 giorni. ⚠️ Non si ricalcolano in silenzio: un ricalcolo storico va versionato e confrontabile, altrimenti sostituisce misure precedenti senza che nessuno possa vedere cosa e' cambiato. **Chiusura**: le 18 righe o vengono rimisurate con provenienza dichiarata, o vengono marcate come non allineate e escluse dalle aggregazioni con la ragione a schermo. |
| FA-069 | P2 | Dati | Una riparazione di base prezzo lascia gli ALERT sulla base vecchia | **DONE / PROD** | Commit `50084a8`. ⚠️ **La voce guardava UNA delle due procedure, e la misura di partenza confrontava la barra sbagliata.** `trigger_price` e' l'ultima chiusura che lo scan aveva, quindi va confrontato con le chiusure della FINESTRA da `signal_date` al giorno della scrittura, non con la sola barra del segnale. Rimisurato cosi': **39 alert su 8 titoli, 11 visibili**, e **20 non vengono da `repair_price_basis` ma dal rebase AUTOMATICO del fetch** — APH e MNST a x2.000 esatto, i 2:1 che `find_basis_breaks` per scelta non vede, e AVB a 2.793, il rapporto che Yahoo dichiara il 17 agosto. La procedura che nessuno lancia a mano era la piu' prolifica. `find_alerts_off_basis` usa la banda dell'ingest (`_BASIS_RATIO_*`) invece di un numero nuovo, e un test pretende che i due concordino ai bordi; le popolazioni non si toccano — p99 dello scarto 0,34%, e sopra il 25% ogni alert e' anche sopra il 50%. Contro la sola barra del segnale MRNA leggeva x2.29 ed e' una rilevazione tardiva dopo un movimento vero, al centesimo. `repair_price_basis` stampa l'elenco a ogni corsa, anche con «nessuna discontinuita'» — il caso tipico DOPO una riparazione — e `_rebase_full_history` avverte nel log quanti alert lascia. **La correzione opzionale NON e' stata costruita**, per tre ragioni ciascuna sufficiente e scritte nel docstring: lo snapshot porta livelli propri sulla base vecchia e il playbook li legge insieme al trigger; il fattore sarebbe nostro (la lezione SOXS); gli esiti sono gia' giusti. Controlli negativi su copia per finestra, banda, avviso e stampa; un `db.flush()` e' stato tolto perche' il suo controllo restava verde. ⚠️ AVB e' anche un titolo morto — nessuna barra dal 24 agosto, quarantena a 171 tentativi — e la serie non e' un difetto nuovo: un download fresco la riproduce. |
| FA-070 | P1 | Migrazioni | La catena di migrazioni NON e' eseguibile da zero su Postgres | **DONE / PROD** | Commit `f8e527a`. La voce avvertiva che il numero di difetti non era noto perche' ognuno nasconde il successivo: erano **DUE**, con la stessa forma — un booleano trattato da intero, che SQLite accetta perche' per lui lo E'. `0601129beb3a` dava a `price_alerts.enabled` il default `sa.text("1")`; `98ebae3370fc` aggiungeva `ohlcv_in_pounds` con default `0` e lo confrontava con `= 0`/`= 1` in tre istruzioni (`operator does not exist: boolean = integer`). Ora `sa.true()`/`sa.false()` e parametri booleani legati dal driver. ⚠️ Modificare migrazioni storiche e' sicuro qui per una ragione VERIFICATA: su SQLite i due costrutti compilano come prima — la catena da vuoto arriva alla testa e i default registrati restano `'1'` e `'0'` — e nessun database gia' migrato riesegue quei corpi. Iterato su un Postgres 16 vero da un ramo di prova con un workflow ridotto (Docker Desktop non partiva in modo non interattivo): tutta la catena in 2,8 secondi. Controlli negativi eseguiti per ENTRAMBE le correzioni separatamente, perche' la catena rossa si ferma alla prima: ciascuna riproduce il proprio errore alla lettera. Il test non si ferma al «non solleva»: la revisione registrata e' la testa e nessuna tabella dei modelli manca. E il test di FA-061 ora sale con la catena vera invece di partire da una tabella a tre colonne scritta a mano. |
| FA-071 | P2 | Dati | Dodici titoli morti sono ancora nell'universo, con punteggi su prezzi fermi | **DONE** | Commit `e460dd2`. ⚠️ **Il danno era molto peggiore di come questa voce lo descriveva quando fu aperta**, e nella direzione che nessuno si aspetta: i titoli morti NON finiscono in fondo alla classifica, finiscono in **cima**. BK 81,9 «Forte», CPRX 81,2, TERN 81,0, APLS 80,9 — al **98esimo percentile** del Tecnico, calcolati OGGI su prezzi fermi dal 15 maggio. Una serie ferma ha volatilita' nulla e trend stabile, quindi produce un punteggio LUSINGHIERO: e' la stessa forma con cui TIT.MI, dopo un raggruppamento non riparato, divento' la forza relativa piu' alta dell'universo. **E ha chiuso una domanda aperta da FA-062**: gli OTTO alert piu' revisionati dell'intero catalogo erano tutti su titoli morti, fino a **372 revisioni** — la scansione rivalutava la stessa ultima barra congelata, il ramo di aggiornamento in cooldown scattava, `triggered_at` veniva spinto a oggi, e la lista ordina per `triggered_at` decrescente: stavano in cima alla posta in arrivo ogni giorno. **Una guardia sola a monte** invece di N esclusioni nei punti di lettura, con tre conseguenze gestite esplicitamente perche' altrimenti si sostituisce un difetto con un altro: (a) `technical_score_service.forget`, perche' `finalize` fa UPSERT e non cancella mai — smettere di valutare avrebbe CONGELATO BK al 98esimo percentile; riceve una lista ESPLICITA di id, mai «tutti quelli fuori da partials», o una scansione ristretta svuoterebbe la classifica. (b) `REASON_NO_DATA`, **la quarta ragione che FA-061 aveva nominato** e che non fu costruita per mancanza di popolazione — senza, questi finirebbero in `stale` = «le condizioni si sono sfaldate», falso: non sono decadute, e' il titolo che ha smesso di quotare. (c) l'ORDINE: la chiusura per dati mancanti precede la scadenza, e il test sull'ordine che era gia' li' e' diventato rosso — il suo mestiere. ⚠️ Il predicato ha ora un proprietario unico, tenuto DISTINTO da `not_quarantined_clause` (che porta il termine su REPROBE_DAYS e risponde a un'altra domanda), con un test che pretende che le forme SQL e Python selezionino le stesse righe. **Effetto dichiarato alla prossima scansione**: 12 titoli fuori dal giro, i loro punteggi Tecnico cancellati, 9 setup chiusi come `no_data` — tutto reversibile da solo, perche' lo streak si azzera al primo fetch riuscito. **Non toccato di proposito**: il punteggio Qualita', che poggia sui fondamentali e non sui prezzi, e il magazzino esiti, che e' storia. **Misura originaria**: Emersa misurando FA-062, e **fuori dal suo perimetro di proposito**. La quarantena OHLCV del 2026-08-26 ha smesso di scaricarli e di chiederne le quotazioni; il resto dell'app non se n'e' accorta. Misurato in produzione il 2026-09-14 — 12 titoli su 1.010, la cui serie si e' fermata fra il 7 maggio e il 24 agosto: **12 portano un punteggio Qualita', 12 un punteggio Tecnico** (cioe' una postura calcolata su prezzi vecchi fino a quattro mesi, che entra nella classifica e nell'ampiezza) **e 9 hanno un setup APERTO**. Zero posizioni aperte, 47 alert ancora visibili. La fonte conferma che sono morti: nove non hanno piu' barre, tre (WBS, EQR, AVB) si fermano al giorno dove ci fermiamo noi. ⚠️ **Non e' «cancellarli»**: la loro storia regge alert, esiti maturati e la meta' negativa del tasso di conversione — e il magazzino esiti e' l'unica fonte sull'efficacia del motore. La domanda e' quali letture devono ESCLUDERLI (classifiche, ampiezza, schermata titoli) e quali no (storia, esiti). ⚠️ E il presidio di freschezza del drill di ripristino non puo' vederlo: guarda `max(ohlcv_daily.date)` GLOBALE, che legge oggi ed e' corretto. Un sottoinsieme fermo e' invisibile a una misura d'insieme — stessa forma del CronJob di parita' che leggeva `.items[0]`. **Chiusura**: una nozione di titolo non piu' quotato leggibile fuori dall'ingestione, e una decisione dichiarata per ciascuna lettura. |
| FA-072 | P3 | Segnali | «Processa segnali» non aggiorna la lista globale degli alert | **DONE / PROD** | Commit `e616a6e`. Invalidava solo il dettaglio, e restava vecchia anche la scheda «Storico completo» della STESSA card, che ha una chiave sua. ⚠️ **Non finge una scansione dell'universo**: quella transizione porta il toast «Scan completato» e la contabilita' di fine giro, che uno scan locale per perimetro non deve fare (FA-062); un test pretende che lo stato dello scan non venga scritto. `useScanStock` possiede l'elenco — dettaglio, lista, storico, alert per id (lo scan puo' aggiornare lo snapshot di uno esistente), compagni dell'ampiezza, setup, confluenze, riepilogo — e un censimento sulle radici delle chiavi di query pretende che ogni radice che parla di alert, setup o confluenze sia coperta o esentata con una ragione, perche' un elenco a mano invecchia alla prossima query nuova. |
| FA-073 | P1 | Accessibilita | L'ambra dell'avvertimento era a 3,19:1 in ventuno file | **DONE / PROD** | Commit `db2ed97`, bundle servito verificato dentro il pod: zero `text-amber-600`, 64 `text-amber-700`. Emersa eseguendo in locale il gate di FA-068: una violazione di contrasto su /stocks che restava identica togliendo ogni fondo, cioe' non del seme. `amber-600` era gia' stato corretto nei toni di punteggio, e la correzione era rimasta dentro quel file: la stessa coppia stava in altri ventuno, piu' un pulsante `bg-amber-600` con testo bianco. 27 sostituzioni, solo la meta' chiara (`amber-400` sul fondo scuro gia' sopra soglia), icone comprese di proposito. Il test rifa l'aritmetica WCAG sui fondi reali — compresi `amber-500/10` e `amber-100/60` misurati sul MISCUGLIO — e censisce i sorgenti, col pavimento sul rimpiazzo: cancellare le classi passerebbe il censimento e toglierebbe l'avvertimento. ⚠️ Il gate l'aveva vista una volta sola e non per merito del codice: vedi FA-074. |
| FA-074 | P1 | Verifica | Il gate UI misurava anche cio' che la rete restituiva quel giorno | **DONE / PROD** | Commit `e2cb561`, `7be9e1f`, `788b294`. Ha bloccato il rilascio di FA-070 — solo migrazioni — con `button-name: 46 -> 49` su /alerts alle 00:01 UTC, verde tredici minuti prima. **All'avvio lo scan di recupero scaricava barre VERE per i ticker del seme e le incollava sopra la storia sintetica**: su /stocks la chiusura vera di 0016.HK (HK$110) sopra una serie da 146 dava RSI 18,8 e la violazione chiusa da FA-073. Spegnerlo e basta avrebbe svuotato le pagine, quindi il seme esegue la meta' che non scarica — `run_tracked_scan` sulle barre memorizzate piu' la contabilita' dei setup — e il job toglie la rete al backend con un proxy su una porta chiusa, piu' `SCAN_STARTUP_STALE_HOURS=0` esplicito. Gate locale senza rete: 50 verdi e ogni conteggio a11y SCENDE (/alerts 46 -> 39); la linea di base non e' stretta da una corsa sola. ⚠️ **Due errori miei, entrambi senza un job rosso a segnalarli**: `HTTPS_PROXY` e `https_proxy` nella stessa mappa `env` — GitHub rifiuta i nomi uguali a meno delle maiuscole e il workflow NON PARTE, zero job, mentre un parser YAML lo dichiarava valido; e un censimento di FA-062 sui chiamanti della contabilita', perche' avevo rieseguito la suite backend prima di toccare il seme e non dopo. ⚠️ **Residuo dichiarato**: `app/core/public_http.py` usa `urllib3` diretto, che non legge le variabili di proxy; scarica il corpo degli articoli e nel seme non ci sono notizie. |
| FA-066 | P2 | UX | Riorganizzare il dettaglio titolo attorno al percorso dell'evento | **DONE / PROD** | **Collaudo in browser eseguito il 2026-09-16** (app locale, database di sviluppo migrato, 1440 / 768 / 375 px), rilascio verificato sul pod `48e6d46a`. Ha trovato cinque difetti, tutti invisibili a 737 test: (1) nel dettaglio titolo le righe dei segnali non avevano un solo elemento focalizzabile, quindi da tastiera un segnale non si apriva; (2) chiuso un dialogo aperto da uno stato il focus finiva sul BODY — quasi tutti i dialoghi dell'app — e i pulsanti «segnale» diventavano `disabled` proprio all'apertura (`6a2daaa2`); (3) tornando indietro da un titolo i filtri di In formazione si perdevano, e (4) a 375px il ticker delle righe di Esiti era ridotto a una lettera, con le etichette delle tessere troncate (`6889732c`); (5) `<main>` e' lo stesso elemento per ogni pagina e nessuno ne gestiva lo scorrimento: un titolo aperto da una lista a 2.500px partiva a 2.500px, e la lista al ritorno stava in fondo (`48e6d46a`). Misurato dopo le correzioni: Esc riporta il focus sulla riga, «Mostra sul grafico» lascia il grafico in vista a desktop (450–900px) e a 375px (362–812px) senza che il focus restituito riporti indietro la pagina, filtri e posizione (2.500 → 0 → 2.500) sopravvivono al ritorno, nessun elemento oltre il bordo a 375 e 768px. ⚠️ Due limiti dello strumento, dichiarati: Invio e Spazio arrivano alla pagina con `key` vuoto, quindi l'attivazione da tastiera e' verificata con un evento `keydown` e con i test, non con un tasto fisico; e con il pannello nascosto lo scorrimento `smooth` non avanza, quindi e' misurato con lo scorrimento istantaneo. **Storia precedente:** Commit `f9817ed` (/setups) e `50f29ee` (dettaglio titolo). **Su /setups** le otto statistiche e la tabella per detector vivono in «Misurazione»: davanti alla lista restano cosa manca — il titolo del gruppo, gia' da prima — l'attesa e, nuova, l'ultima osservazione, mostrata solo da quattro giorni per la stessa ragione della pastiglia «in ritardo»: una scansione notturna attraverso un fine settimana accenderebbe ogni riga il lunedi'. Vista e titolo vivono nell'URL (`vista=esiti|misurazione`, `ticker=`), e in «Misurazione» con un titolo filtrato la pagina dice che le misure sono dell'intera funzione, perche' `conversion_stats` non guarda i filtri. **Sul dettaglio titolo** ogni alert porta `setup_origin` in tutti e tre i percorsi — lista, alert singolo, dettaglio — con UNA query per pagina: il collegamento esisteva da FA-061 ma usciva solo dalla lista setup, paginata a cinquanta righe, e cercarlo li' avrebbe mancato in silenzio ogni episodio oltre. Il dialogo dice da quale setup e' nato e con quanto anticipo, porta agli esiti dei setup del titolo, e offre «Mostra sul grafico» solo dove un grafico c'e', con la stessa regola dei marker. ⚠️ **Il test di parita' con i marker era vero di niente alla prima stesura**: sulle barre giornaliere «prima barra del giorno» e «barra che contiene il giorno» coincidono sempre, quindi scambiarle passava. Ora ha due casi intraday, dove FA-063 le ha separate. La scheda storica porta a `/setups?ticker=`; la scheda setup rimossa non torna, e il gate e2e lo pretende. **Non fatto, e detto**: il riepilogo con i «risultati disponibili» — sui recenti gli esiti sono quasi sempre zero perche' stanno sugli archiviati (FA-054), e un contatore a zero accanto a uno storico pieno direbbe il contrario del vero; e la selezione dalla riga della tabella al grafico, che passa dal dialogo invece di un'evidenziazione. ⚠️ **Resta aperta sul quinto criterio**: collaudo su browser reale, desktop e mobile, tastiera e ritorno. jsdom non fa layout ne' scorrimento: `scrollIntoView` del grafico su un telefono e la larghezza della colonna dell'attesa a 768px non li esercita nessun test. |

⚠️ **FA-006 e FA-007 non sono lavoro rimandato, sono misure che maturano.**
Trattarle come task da spingere significa concludere su campioni che non hanno
ancora senso — lo stesso errore che il magazzino esiti rende esplicito sullo
schermo con "non concludente".

FA-011 e FA-013 sono le uniche due che aspettano l'utente: il codice e in
produzione, manca il collaudo su un dispositivo vero, che jsdom non puo fare
(CLAUDE.md: senza stili calcolati axe non vede focus visibile ne target touch).

Le 31 voci mancanti dell'audit UI/UX **non sono in questa lista di proposito**.
Sono candidate riconciliate, non lavoro impegnato: prendono un ID qui quando
l'utente ne chiede una.

## Evidenze e limiti

- Il 2026-09-09 il cluster e stato verificato via SSH: CNPG 1.30.0, plugin Barman installato; nessun aggiornamento dell'operatore eseguito.
- La pipeline CI 34366785799 e verde; produzione verificata sull'immagine `42693fb` con Argo `Synced/Healthy`. Gli aggiornamenti successivi richiedono la propria verifica CI/CD.
- La revoca sessioni e' persistita in `revoked_sessions` e resiste ai riavvii (FA-014 DONE / PROD); le letture restano in memoria, quindi con piu' repliche la revoca raggiunge l'altra al suo riavvio. Questa riga ha detto «resta aperto» dopo la chiusura.
- I valori RUM raccolti dal precedente reporter non vanno usati come Core Web Vitals validati; attendere la versione corretta e campioni reali.
- Il Python globale non contiene le dipendenze backend: usare uv run --project backend pytest.
